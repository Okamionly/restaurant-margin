# Bug Fixer — 2026-04-26

**Heure** : 11h00 Paris  
**Statut** : ✅ Fix déployé

---

## Bug fixé

**Catégorie** : Sécurité — P0  
**Source** : Rapport CTO du 2026-04-24 (section "Endpoint de debug Stripe exposé en production")

### Problème

L'endpoint `GET /api/debug/stripe-fetch` avait été ajouté le commit `44ebaab` pour diagnostiquer un problème réseau Stripe. Une fois le problème Stripe résolu (`1c4bda7`), cet endpoint est resté en production et exposait :

- La présence ou non de `STRIPE_SECRET_KEY` dans les variables d'env (`hasKey: true/false`)
- Des détails d'erreur réseau internes (codes d'erreur Node.js, DNS, causes)
- Une surface d'attaque permettant de tester la connectivité Stripe depuis l'infra prod sans aucune authentification

**Fichier** : `api/index.ts`  
**Lignes supprimées** : 987–1010 (bloc de 24 lignes)

### Avant

```typescript
// ── DEBUG Stripe connectivity (temporary) ──
app.get('/api/debug/stripe-fetch', async (_req: any, res) => {
  const results: any = { nodeVersion: process.version, hasKey: !!process.env.STRIPE_SECRET_KEY };
  // Test 1: raw fetch to api.stripe.com
  try {
    const t0 = Date.now();
    const r = await fetch('https://api.stripe.com/v1/balance', {
      headers: { Authorization: `Bearer ${process.env.STRIPE_SECRET_KEY}` },
    });
    const body = await r.json() as any;
    results.rawFetch = { status: r.status, tookMs: Date.now() - t0, livemode: body?.livemode };
  } catch (e: any) {
    results.rawFetch = { error: e.message, code: e.code, cause: e.cause?.message, causeCode: e.cause?.code };
  }
  // Test 2: DNS resolution
  try {
    const dns = await import('dns/promises');
    const addrs = await dns.resolve4('api.stripe.com');
    results.dns = { ips: addrs.slice(0, 3) };
  } catch (e: any) {
    results.dns = { error: e.message };
  }
  res.json(results);
});
```

### Après

Bloc entièrement supprimé. Route inexistante — retourne 404 comme attendu.

---

## Commit

`3851d41` — `fix(security): remove /api/debug/stripe-fetch endpoint exposed en prod`

---

## Autres observations

- TODO à `api/index.ts:2154` : modèles RFQ pas encore créés dans schema.prisma — non bloquant, commentaire de garde en place.
- Build TypeScript : faux positifs environnementaux (node_modules absents dans l'env d'audit) — pas de régression réelle.
- API 500 en prod : problème BDD Supabase/PostgreSQL inaccessible depuis Vercel — hors scope bug fixer (infra).
