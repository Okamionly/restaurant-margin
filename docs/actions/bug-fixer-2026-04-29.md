# Bug Fixer — 2026-04-29

## Source
- `docs/reports/cto-last.md` (rapport 2026-04-24) : P0 debug/stripe-fetch → déjà supprimé dans commits précédents
- `docs/reports/qa-last.md` : API 503/500 → infra Supabase, non fixable depuis le code
- Analyse manuelle : régression UX post-retrait fake data (commit 2026-04-28)

## Bug fixé

**Fichier** : `client/src/pages/Settings.tsx`

**Contexte** : le commit du 2026-04-28 a retiré les fake data hardcodées (2 sessions Chrome/Safari fictives, 3 factures fictives). Résultat : les sections "Sessions actives" et "Historique de facturation" sont désormais vides sans aucun feedback visuel.

### Fix 1 — Sessions actives (autour ligne 1540)

**Avant** : bouton "Deconnecter tous les appareils" toujours visible, toujours cliquable, affiche un toast de succès même quand `activeSessions` est vide (no-op trompeur).

**Après** :
- Bouton conditionnel : visible uniquement si `activeSessions.filter(s => !s.current).length > 0`
- Empty state : `"Aucune session active detectee"` quand le tableau est vide

### Fix 2 — Historique de facturation (autour ligne 1718)

**Avant** : table avec headers mais aucune ligne — rendu vide sans message, UX ambiguë (bug ou donnée absente ?)

**Après** :
- Empty state dans le `<tbody>` : ligne `colspan="4"` avec `"Aucune facture disponible"`

## Commit
`a5a9ba2` — fix(settings): empty states sessions/facturation apres retrait fake data
