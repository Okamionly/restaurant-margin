# Bug Fixer — 2026-05-28

## Statut : FIX APPLIQUÉ ✅

## Sources consultées
- `docs/reports/cto-last.md` (2026-05-16) — services null déjà corrigé, TODOs Settings.tsx déjà implémentés
- `docs/reports/qa-last.md` (2026-05-27) — 15/15 pages OK, 0 erreur TS, aucune régression
- `grep TODO/FIXME/HACK` — 2 TODOs trouvés (LaunchPH embed Loom placeholder, api/index.ts RFQ schema)

## Bug choisi : Toasts hardcodés en français (i18n)

**Type** : bug i18n — 4 messages toast en pur français dans une app 5 langues  
**Fichier** : `client/src/pages/Recipes.tsx`  
**Impact** : utilisateurs anglais/arabes/espagnols/allemands voient les messages en français

## Changements

### `client/src/pages/Recipes.tsx`

| Ligne | Avant | Après |
|-------|-------|-------|
| 2106 | `` showToast(`${ids.length} recette(s) supprimee(s)`, 'success') `` | `showToast(t("recipes.recipeDeleted"), 'success')` |
| 2137 | `` showToast(`Categorie changee pour ${ids.length} recette(s)`, 'success') `` | `showToast(t("recipes.categoryUpdated"), 'success')` |
| 2356 | `showToast('Entrez un nom de recette (min 2 caracteres)', 'error')` | `showToast(t("recipes.nameRequired"), 'error')` |
| 2636 | `showToast('Formulaire pre-rempli avec les ingredients', 'success')` | `showToast(t("recipes.formPrefilled"), 'success')` |

### `client/src/locales/fr.json` — 3 nouvelles clés ajoutées
```json
"recipes.categoryUpdated": "Catégorie mise à jour",
"recipes.nameRequired": "Entrez un nom de recette (min. 2 caractères)",
"recipes.formPrefilled": "Formulaire pré-rempli avec les ingrédients"
```

### `client/src/locales/en.json` — 3 nouvelles clés ajoutées
```json
"recipes.categoryUpdated": "Category updated",
"recipes.nameRequired": "Enter a recipe name (min. 2 characters)",
"recipes.formPrefilled": "Form prefilled with ingredients"
```

> es/de/ar : pas de clés recipes dans ces locales, fallback automatique sur fr (hook useTranslation).

## Commit
`6da981c fix(i18n): traduire 4 toasts hardcodés en français dans Recipes.tsx`
