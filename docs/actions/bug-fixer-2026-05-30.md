# Bug Fixer — 2026-05-30

## Statut : FIX APPLIQUÉ ✅

## Sources consultées
- `docs/reports/cto-last.md` (2026-05-16) — health endpoint déjà corrigé, TODOs Settings.tsx déjà implémentés, dette technique `api/index.ts` monolithique
- `docs/reports/qa-last.md` (2026-05-29) — 15/15 pages OK, 0 erreur TS, aucune régression
- `grep TODO/FIXME/HACK` — 2 TODOs trouvés non actionnables (LaunchPH embed Loom sans vidéo, RFQ nécessite schema Prisma)

## Bug choisi : 6 toasts hardcodés en français dans `AutoOrders.tsx`

**Type** : incohérence i18n — toasts en dehors du système de traduction  
**Fichier** : `client/src/pages/AutoOrders.tsx`  
**Lignes** : 1113, 1198, 1242, 1245, 1568, 1576  
**Contexte** : La page `AutoOrders` utilisait déjà `t()` pour la majorité de ses messages (ex: `loadError`, `selectSupplier`, `orderModified`…), mais 6 toasts avaient été écrits directement en français sans passer par le hook `useTranslation`.

## Changements

### `client/src/pages/AutoOrders.tsx` — 6 lignes

| Ligne | Avant | Après |
|-------|-------|-------|
| 1113 | `showToast('Erreur changement de statut', 'error')` | `showToast(t('autoOrders.statusChangeError'), 'error')` |
| 1198 | `showToast('Commande re-creee en brouillon', 'success')` | `showToast(t('autoOrders.orderResetToDraft'), 'success')` |
| 1242 | `showToast('Commande receptionnee, inventaire mis a jour', 'success')` | `showToast(t('autoOrders.orderReceived'), 'success')` |
| 1245 | `showToast('Erreur lors de la reception', 'error')` | `showToast(t('autoOrders.receptionError'), 'error')` |
| 1568 | `showToast('Commande renouvelee via WhatsApp', 'success')` | `showToast(t('autoOrders.orderRenewedWhatsApp'), 'success')` |
| 1576 | `showToast('Aucune commande en brouillon a envoyer', 'error')` | `showToast(t('autoOrders.noDraftOrders'), 'error')` |

### `client/src/locales/fr.json` — 6 clés ajoutées dans `autoOrders`

```json
"statusChangeError": "Erreur lors du changement de statut",
"orderResetToDraft": "Commande recréée en brouillon",
"orderReceived": "Commande réceptionnée, inventaire mis à jour",
"receptionError": "Erreur lors de la réception",
"orderRenewedWhatsApp": "Commande renouvelée via WhatsApp",
"noDraftOrders": "Aucune commande en brouillon à envoyer"
```

### `client/src/locales/en.json` — 6 clés ajoutées dans `autoOrders`

```json
"statusChangeError": "Error updating order status",
"orderResetToDraft": "Order reset to draft",
"orderReceived": "Order received, inventory updated",
"receptionError": "Error during reception",
"orderRenewedWhatsApp": "Order renewed via WhatsApp",
"noDraftOrders": "No draft orders to send"
```

**Raison** : Cohérence du système i18n. Les toasts hardcodés cassaient la logique de traduction (même si la locale est forcée sur `fr` pour l'instant, la bonne pratique s'impose pour la maintenabilité et la future ouverture multilingue).

## Commit
`32e6eee fix(i18n): remplacer 6 toasts hardcodés en français dans AutoOrders par t()`
