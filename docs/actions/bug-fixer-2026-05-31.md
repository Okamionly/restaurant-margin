# Bug Fixer — 2026-05-31

## Statut : ✅ Fix déployé

---

## Source de détection

Grep des toasts dans `client/src/pages/MenuCalendar.tsx` — aucun bug CTO/QA critique signalé (code sain d'après les rapports).

---

## Bug corrigé : Accents manquants dans les messages toast de MenuCalendar

**Fichier** : `client/src/pages/MenuCalendar.tsx`

**Problème** : 5 chaînes de caractères françaises avec accents absents, rendant l'interface orthographiquement incorrecte.

### Lignes modifiées

| Ligne | Avant | Après |
|-------|-------|-------|
| 392 | `ajoute au` | `ajouté au` |
| 462 | `deplace au` | `déplacé au` |
| 464 | `deplacement` | `déplacement` |
| 494 | `Total estime:` | `Total estimé:` |
| 503 | `exportee` | `exportée` |

**Raison** : Toasts visibles par l'utilisateur final lors des actions d'ajout de recette au calendrier, de drag & drop, et d'export de la liste de courses. Fautes visibles = image dégradée du produit.

---

## Commit

```
fix: accents manquants dans les toasts de MenuCalendar (ajouté, déplacé, exportée)
```

## LOC modifiées : 5
## Durée : < 5 min
