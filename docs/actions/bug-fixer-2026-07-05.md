# Bug Fixer — 2026-07-05

## Statut : FIX APPLIQUÉ

---

## Rapport d'entrée

- **CTO (2026-07-04)** : 0 TODO/FIXME/HACK actifs, 0 erreur TypeScript réelle, dette principale = api/index.ts monolithique (6 442 LOC) + 162 pages à auditer
- **QA (2026-07-04)** : 15/15 pages OK, build OK, 0 régression
- **TODO/FIXME/HACK grep** : 0 résultat

---

## Bug détecté

**Fichier** : `client/src/pages/InvoiceScanner.tsx`  
**Nature** : Dead code — fonction `handleScanImport` (82 lignes) jamais appelée  
**Lignes concernées (avant fix)** : 934–1014  

### Contexte

Lors du refacto des factures (commits `feat(factures)` du 2026-07-03/04), le handler d'import a été migré vers un appel transactionnel `/api/invoices/import` via `handleImportInvoice`. L'ancienne implémentation `handleScanImport` (qui appelait séquentiellement `/api/invoices` puis `updateIngredient` en boucle) n'a jamais été supprimée.

Résultat : 82 lignes de code mort dans le bundle, source de confusion pour les futurs agents.

### Avant

```typescript
// L'ancien handler — jamais appelé dans le JSX
const handleScanImport = async () => {
  // POST /api/invoices (ancien endpoint)
  // loop sur matchedItems → updateIngredient() un par un
  // pas de gestion d'inflation, pas de review rows
  // 82 lignes
};

// Le nouveau handler — seul appelé à la ligne 1643
const handleImportInvoice = async () => {
  // POST /api/invoices/import (transactionnel)
  // review rows, inflation threshold, price history
};
```

### Après

`handleScanImport` supprimé. Seul `handleImportInvoice` existe, branché sur le bouton "Valider et importer" (ligne 1561 post-fix).

---

## Vérification

- `grep handleScanImport client/src/pages/InvoiceScanner.tsx` → 0 résultat
- `updateIngredient` conservé (encore utilisé dans OCR legacy et handleImportInvoice)
- Fichier : 2 495 lignes (was 2 577)

---

## Commit

```
da848c9 fix(InvoiceScanner): suppression handleScanImport — dead code 82 lignes post-refacto /api/invoices/import
```

Pushé sur `main`.
