# Bug Fixer — 2026-08-01

## Statut : ✅ FIX APPLIQUÉ

---

## Diagnostic

- **CTO last** : 0 TODO/FIXME, 0 erreur TypeScript, dette = monolithe api/index.ts
- **QA last** : 15/15 pages OK, build clean (rapport 2026-07-31)
- **grep TODO/FIXME/HACK** : 0 résultat

→ Scan étendu : grep des `showToast()` codés en dur dans les pages principales

---

## Bug sélectionné

**12 messages toast hardcodés en français dans `Ingredients.tsx`**

Même pattern que le fix du 2026-07-31 (`useUndoDelete`). Les messages de feedback
utilisateur pour les opérations bulk et l'import/export CSV ignoraient le système i18n,
rendant la page Ingrédients non-localisable pour les utilisateurs EN/ES/DE/AR.

---

## Fichier corrigé

### `client/src/pages/Ingredients.tsx`

| Ligne | Avant | Après |
|-------|-------|-------|
| 491 | `showToast(\`Alerte configuree : ${...}\`, ...)` | `showToast(t('ingredients.alertSet').replace(...)` |
| 494 | `showToast(\`Alerte supprimee pour ${...}\`, ...)` | `showToast(t('ingredients.alertRemoved').replace(...)` |
| 857 | `showToast(\`${ids.length} ingredient(s) supprime(s)\`, ...)` | `showToast(t('ingredients.bulkDeleted').replace(...)` |
| 871 | `showToast(\`Categorie changee pour ${ids.length}...\`, ...)` | `showToast(t('ingredients.bulkCategoryChanged').replace(...)` |
| 884 | `showToast(\`Fournisseur assigne pour ${ids.length}...\`, ...)` | `showToast(t('ingredients.bulkSupplierAssigned').replace(...)` |
| 919 | `showToast(\`Prix mis a jour pour ${...}\`, ...)` | `showToast(t('ingredients.bulkPriceUpdated').replace(...)` |
| 948 | `showToast(\`${ingredients.length} ingredients exportes en CSV\`, ...)` | `showToast(t('ingredients.csvExported').replace(...)` |
| 973 | `showToast(\`${selectedIngs.length} ingredient... exporte...en CSV\`, ...)` | `showToast(t('ingredients.csvExported').replace(...)` |
| 1506 | `showToast('Le fichier CSV est vide ou invalide', ...)` | `showToast(t('ingredients.csvEmpty'), ...)` |
| 1513 | `showToast('Colonne "Nom" introuvable dans le CSV', ...)` | `showToast(t('ingredients.csvColumnMissing'), ...)` |
| 1533 | `showToast(\`${importCount} ingredient... importe...avec succes\`, ...)` | `showToast(t('ingredients.csvImported').replace(...)` |
| 1534 | `showToast('Erreur lors de la lecture du fichier CSV', ...)` | `showToast(t('ingredients.csvReadError'), ...)` |

### Clés ajoutées dans les 5 locales (fr/en/es/de/ar)

7 nouvelles clés dans la section `ingredients` :
- `alertSet` — "Alerte configurée : {name} > {price} €"
- `alertRemoved` — "Alerte supprimée pour {name}"
- `bulkDeleted` — "{count} ingrédient(s) supprimé(s)"
- `bulkCategoryChanged` — "Catégorie changée pour {count} ingrédient(s)"
- `bulkSupplierAssigned` — "Fournisseur assigné pour {count} ingrédient(s)"
- `bulkPriceUpdated` — "Prix mis à jour pour {count} ingrédient(s)"
- `csvColumnMissing` — "Colonne \"Nom\" introuvable dans le CSV"

5 clés déjà existantes correctement utilisées :
`csvExported`, `csvEmpty`, `csvImported`, `csvReadError`, `csvColumnMissing`

---

## Commit

`4f6a522` — `fix: i18n des 12 toasts hardcodés FR dans Ingredients.tsx`

6 fichiers modifiés, 52 insertions, 17 suppressions.
