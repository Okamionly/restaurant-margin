# Bug Fixer — 2026-08-20

## Statut : ✅ Fix appliqué

## Contexte
- CTO : 0 TODO/FIXME/HACK, 0 erreurs TypeScript
- QA : 15/15 pages OK, aucune régression

## Bug détecté
**6 chaînes françaises hardcodées dans les toasts de `Recipes.tsx`** — messages non traduits affichés en français brut, sans passer par le hook `useTranslation`.

## Fichiers modifiés

### `client/src/pages/Recipes.tsx`
| Ligne | Avant | Après |
|-------|-------|-------|
| 2380 | `` `${data.ingredients.length} ingredients suggeres par l'IA` `` | `t('recipes.aiSuggestSuccess').replace('{count}', ...)` |
| 2382 | `'L\'IA n\'a pas pu generer de suggestions...'` | `t('recipes.aiNoSuggestions')` |
| 2386 | `` `Erreur IA: ${msg}` `` | `t('recipes.aiError').replace('{msg}', msg)` |
| 2413 | `` `${selected.length} ingredient(s) ajoute(s)` `` | `t('recipes.ingredientsAdded').replace('{count}', ...)` |
| 4433 | `` `${toUpdate.length} prix mis a jour` `` | `t('recipes.pricesUpdated').replace('{count}', ...)` |
| 4438 | `'Erreur lors de la mise a jour des prix'` | `t('recipes.errorUpdatingPrices')` |

### `client/src/locales/fr.json` + `en.json`
6 nouvelles clés ajoutées dans les deux fichiers :
- `recipes.aiSuggestSuccess`
- `recipes.aiNoSuggestions`
- `recipes.aiError`
- `recipes.ingredientsAdded`
- `recipes.pricesUpdated`
- `recipes.errorUpdatingPrices`

## Raison
Ces toasts étaient affichés en français sans accentuation correcte (`ajoute` au lieu de `ajouté`, `Erreur` au lieu de passer par le système i18n). En cas d'activation d'une autre locale, les messages restaient illisibles.

## Commit
`153d189` — `fix: i18n toasts IA et batch-prix hardcodes dans Recipes.tsx`
