# Bug Fixer — 2026-08-24

## Statut : ✅ Fix appliqué

---

## Analyse

- **CTO last** : 0 erreurs TypeScript, 0 TODO/FIXME. Dette principale = monolithe `api/index.ts` (6 847 lignes) + cold-start 418 ms. Client Anthropic et DB déjà initialisés au boot → cold-start déjà optimisé.
- **QA last** : 15/15 pages OK, build TS propre, 0 régression.
- **Grep TODO/FIXME/HACK** : 0 résultat.

## Bug choisi : Toasts non traduits dans `Seminaires.tsx`

11 chaînes françaises hardcodées dans `showToast(...)` au lieu d'utiliser le hook `t()`.

---

## Fix appliqué

**Fichier** : `client/src/pages/Seminaires.tsx`

| Ligne | Avant | Après |
|-------|-------|-------|
| 202 | `showToast('Erreur chargement séminaires', 'error')` | `showToast(t('seminaires.errorLoading'), 'error')` |
| 327 | `showToast('Statut mis à jour', 'success')` | `showToast(t('seminaires.statusUpdated'), 'success')` |
| 329 | `showToast('Erreur mise à jour statut', 'error')` | `showToast(t('seminaires.errorUpdatingStatus'), 'error')` |
| 336 | `showToast('Veuillez remplir le nom du client et la date', 'error')` | `showToast(t('seminaires.errorRequiredFields'), 'error')` |
| 366 | `showToast('Événement créé avec succès', 'success')` | `showToast(t('seminaires.eventCreated'), 'success')` |
| 368 | `showToast('Erreur création événement', 'error')` | `showToast(t('seminaires.errorCreating'), 'error')` |
| 404 | `showToast(action === 'envoyer_devis' ? 'Devis envoyé par email' : 'Action effectuée', 'success')` | `showToast(action === 'envoyer_devis' ? t('seminaires.devisSent') : t('seminaires.actionDone'), 'success')` |
| 406 | `showToast('Erreur mise à jour statut', 'error')` | `showToast(t('seminaires.errorUpdatingStatus'), 'error')` |
| 419 | `showToast('Événement supprimé', 'success')` | `showToast(t('seminaires.eventDeleted'), 'success')` |
| 421 | `showToast('Erreur suppression', 'error')` | `showToast(t('seminaires.errorDeleting'), 'error')` |
| 1445 | `showToast('Impression du BEO en cours...', 'info')` | `showToast(t('seminaires.printingBeo'), 'info')` |

**Locales mises à jour** : `fr.json`, `en.json`, `es.json`, `ar.json`, `de.json`  
Clé ajoutée : `"seminaires"` avec 11 sous-clés dans chaque fichier.

## Commit

```
d502e5c fix: traduction des toasts hardcodés dans Seminaires.tsx (11 strings → i18n)
```

Pushé sur `main`.
