# Bug Fixer — 2026-09-12

**Heure :** 11h00 Paris  
**Statut :** ✅ Fix appliqué

---

## Bug détecté

**Type :** Toasts i18n manquants (5 strings hardcodées en français)  
**Sévérité :** Mineure — les messages d'erreur CSV et de suppression de données ne se traduisent pas selon la langue de l'utilisateur.

### Fichiers concernés

#### `client/src/pages/Clients.tsx` (lignes 1367, 1373 avant fix)

| Avant | Après |
|-------|-------|
| `showToast('Le fichier CSV est vide ou invalide', 'error')` | `showToast(t('clients.csvEmpty'), 'error')` |
| `showToast('Colonne "Nom" ou "Prenom" introuvable dans le CSV', 'error')` | `showToast(t('clients.csvColumnMissing'), 'error')` |

#### `client/src/pages/Settings.tsx` (lignes 836, 838, 845 avant fix)

| Avant | Après |
|-------|-------|
| `showToast('Export CSV telecharge', 'success')` | `showToast(t('settings.csvExported'), 'success')` |
| `showToast("Erreur lors de l'export CSV", 'error')` | `showToast(t('settings.csvExportError'), 'error')` |
| `showToast('Toutes les donnees ont ete supprimees', 'success')` | `showToast(t('settings.allDataDeleted'), 'success')` |

---

## Clés i18n ajoutées

### `client/src/locales/fr.json`
```json
"clients.csvEmpty": "Le fichier CSV est vide ou invalide",
"clients.csvColumnMissing": "Colonne \"Nom\" ou \"Prenom\" introuvable dans le CSV",
"settings.csvExported": "Export CSV téléchargé",
"settings.csvExportError": "Erreur lors de l'export CSV",
"settings.allDataDeleted": "Toutes les données ont été supprimées"
```

### `client/src/locales/en.json`
```json
"clients.csvEmpty": "The CSV file is empty or invalid",
"clients.csvColumnMissing": "\"Name\" or \"First name\" column not found in CSV",
"settings.csvExported": "CSV export downloaded",
"settings.csvExportError": "Error exporting CSV",
"settings.allDataDeleted": "All data has been deleted"
```

---

## Raison

Détecté via grep sur les appels `showToast(` avec des strings hardcodées. Ces messages s'affichaient toujours en français quelle que soit la langue active de l'utilisateur.

## Commit

`0cd2a69` — fix: traduire les toasts hardcodés en français dans Clients et Settings
