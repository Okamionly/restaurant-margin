# Outreach Bot — 2026-07-24

**Heure d'exécution :** 14h00 (Paris)
**Statut :** ⛔ AUCUN EMAIL ENVOYÉ

## Raison

Tous les prospects dans `docs/campaigns/prospects.csv` (50 entrées) sont marqués `[FICTIF]`.

Règle de sécurité appliquée : **"NE PAS utiliser si fake, skip l'envoi et logger"**

## Détail

- Fichier `prospects.csv` : 50 lignes, toutes avec préfixe `[FICTIF]` sur l'email
- Fichier `contacted.csv` : 0 contacts réels enregistrés (entête seul)
- Emails envoyés ce jour : **0**
- Runs consécutifs bloqués : **13** (depuis le 11/05/2026 — 74 jours sans envoi)

## Contexte

Le problème est identique aux runs précédents (voir logs depuis mai 2026). Les fichiers de contacts réels disponibles dans `docs/campaigns/` n'ont pas été qualifiés pour l'envoi.

Des fichiers de contacts potentiels existent :
- `national-contacts.csv` — contacts multi-villes
- `remaining-contacts.csv` — contacts Montpellier et région
- `montpellier-contacts-v2.csv`, `montpellier-region-contacts.csv`, `phase2-contacts.csv`, `phase3-4-contacts.csv`, `scrapling-contacts.csv`

Ces contacts ne sont pas opt-in explicites. Leur utilisation pour de l'emailing commercial requiert une analyse RGPD (art. L.34-5 Code des P&CE) avant tout envoi.

## Action requise (urgente — bot bloqué depuis 74 jours)

### 1. Peupler `prospects.csv` avec de vrais contacts
Remplacer les 50 entrées `[FICTIF]` par des adresses réelles et qualifiées :
- Option A : contacts opt-in via formulaire restaumargin.fr
- Option B : qualification manuelle des contacts Google Maps (vérifier consentement / base légale B2B pro)
- Option C : sourcing LinkedIn ou réseaux pro restauration

Format attendu :
```
email,name,restaurant_name,city,type
contact@vrairestaurant.fr,Prénom Nom,Nom du Restaurant,Ville,type
```

### 2. Rotation de la clé API Resend (⚠️ sécurité critique)
La clé `re_7ubbQbFa_...` est exposée en clair dans la config du bot depuis mai 2026.
→ **Révoquer sur dashboard.resend.com → API Keys → Nouvelle clé → Stocker dans `.env`**
