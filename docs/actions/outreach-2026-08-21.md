# Outreach Bot — 2026-08-21

**Heure d'exécution :** 14h00 (Paris)
**Statut :** ⛔ AUCUN EMAIL ENVOYÉ

## Raison

Tous les prospects dans `docs/campaigns/prospects.csv` (50 entrées) sont marqués `[FICTIF]`.

Règle de sécurité appliquée : **"NE PAS utiliser si fake, skip l'envoi et logger"**

## Détail

- Fichier `prospects.csv` : 50 lignes, toutes avec préfixe `[FICTIF]` sur l'email
- Fichier `contacted.csv` : 0 contacts réels enregistrés (entête seul)
- Emails envoyés ce jour : **0**
- Runs consécutifs bloqués : **16** (depuis le 11/05/2026 — 102 jours sans envoi)

## Ressource disponible non utilisée

`docs/campaigns/remaining-contacts.csv` contient des contacts réels (format restaurant_name;cuisine_type;email;phone;address;city;source).

Le bot ne l'utilise pas car la config pointe uniquement sur `prospects.csv`. À activer manuellement si souhaité.

## Action requise (critique — bot bloqué depuis 102 jours)

Pour débloquer le bot, deux options :

### Option A — Peupler `prospects.csv` avec de vrais contacts
Remplacer les 50 entrées `[FICTIF]` par des adresses réelles sans le préfixe `[FICTIF]`.

Format attendu :
```
email,name,restaurant_name,city,type
contact@vrairestaurant.fr,Prénom Nom,Nom du Restaurant,Ville,type
```

### Option B — Pointer le bot sur `remaining-contacts.csv`
Vrais contacts déjà disponibles. Mettre à jour la config du bot pour lire ce fichier.

### Sécurité (priorité haute)
La clé API Resend est exposée en clair dans la config du bot depuis mai 2026.
→ Révoquer sur dashboard.resend.com → API Keys → Stocker la nouvelle clé dans une variable d'environnement sécurisée.
