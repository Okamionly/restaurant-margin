# Outreach Log — 2026-09-10

**Heure d'exécution :** 14h00 Paris (automatique)
**Statut :** AUCUN EMAIL ENVOYÉ

## Raison

Le fichier `docs/campaigns/prospects.csv` existe et contient 50 entrées, mais **toutes sont marquées `[FICTIF]`** — ce sont des contacts fictifs avec des adresses email invalides (format `[FICTIF]contact@...`).

Conformément à la règle de sécurité de la tâche : *"NE PAS utiliser si fake, skip l'envoi et logger"*, aucun email n'a été envoyé.

## Fichiers de contacts disponibles mais non utilisés

Des fichiers contenant des contacts réels existent dans `docs/campaigns/` mais ne sont pas le fichier source désigné (`prospects.csv`) :
- `national-contacts.csv`
- `phase2-contacts.csv`
- `phase3-4-contacts.csv`
- `montpellier-contacts-v2.csv`
- `remaining-contacts.csv`
- `scrapling-contacts.csv`

## Action requise

Pour débloquer l'envoi automatique, deux options :
1. **Remplacer `prospects.csv`** avec de vrais contacts (format : `email,name,restaurant_name,city,type`)
2. **Indiquer le fichier source** à utiliser parmi les fichiers disponibles ci-dessus

## Prochaine exécution

Demain 14h00 Paris — même comportement si `prospects.csv` n'est pas mis à jour.
