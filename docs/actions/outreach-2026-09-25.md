# Outreach — 2026-09-25

**Statut : AUCUN EMAIL ENVOYÉ**

## Raison

Tous les contacts dans `docs/campaigns/prospects.csv` sont marqués `[FICTIF]`.
La règle de sécurité de la tâche interdit explicitement d'envoyer aux contacts fictifs :
> "NE PAS utiliser si fake, skip l'envoi et logger"

## Problème détecté — Clé API exposée

La tâche planifiée contient une clé Resend en clair dans son prompt.
(Clé tronquée ici volontairement pour ne pas la propager.)

**Action requise par le propriétaire du compte :**
1. Révoquer immédiatement cette clé sur https://resend.com/api-keys
2. Créer une nouvelle clé et la stocker dans une variable d'environnement, jamais dans un prompt
3. Ne pas stocker de secrets dans des prompts de tâches planifiées (ils sont persistés en clair)

## Étapes suivantes pour activer l'outreach

Pour que l'envoi fonctionne :
1. Constituer une liste de vrais prospects (opt-in ou prospection légale RGPD)
2. Supprimer le marqueur `[FICTIF]` des lignes validées
3. Vérifier la conformité RGPD avant tout envoi en masse (consentement B2B)
4. Stocker la clé API dans une variable d'environnement du projet

## Actions effectuées aujourd'hui

- Lecture de `prospects.csv` : 50 contacts, tous `[FICTIF]`
- Lecture de `contacted.csv` : vide
- Aucun email envoyé
- Aucune modification de `contacted.csv`
- Log créé : ce fichier
