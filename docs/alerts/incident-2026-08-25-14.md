# Incident RestauMargin — 2026-08-25 14h UTC

## Résumé
- **Composant affecté** : base de données (DB)
- **Statut global** : `degraded` (site racine répond 200, mais `/api/health?deep=1` renvoie un statut dégradé)
- **IA** : OK — pas de problème modèle/clé (`services.ai: "ok"`, modèle actif `claude-sonnet-4-6`)
- **API** : OK

## Détail technique
Requête : `curl -s "https://www.restaumargin.fr/api/health?deep=1"`

```json
{"status":"degraded","timestamp":"2026-08-25T14:00:01.472Z","uptime":452,"responseTime":"1106ms","services":{"database":"error","ai":"ok","aiDetail":"claude-sonnet-4-6","api":"ok"}}
```

- `services.database` = `"error"`
- `uptime: 452` (secondes) — le process a redémarré récemment (~7,5 min avant ce check), donc pas un crash permanent, plutôt une DB inaccessible au démarrage ou une connexion qui échoue.
- Site racine (`GET /`) répond bien HTTP 200 → ce n'est pas une panne totale, mais toute fonctionnalité dépendant de la DB (auth, données resto, etc.) est probablement cassée pour les utilisateurs.

## Continuité avec incident précédent
Un incident identique (même journée, `database:"error"`, `status:"degraded"` HTTP 503) avait déjà été détecté à **13:58 UTC** (2 min avant ce check) mais **jamais notifié** — la tentative d'alerte Pushover avait été rejetée (token absent). Aucun fichier d'alerte n'existait encore dans ce dossier avant celui-ci. Il s'agit donc probablement du même incident encore ouvert, pas d'un nouveau.

## Action recommandée
1. Vérifier le dashboard Supabase du projet RestauMargin (le composant DB Supabase lui-même semblait OK lors du premier check à 13:58 — donc suspecter la couche app→DB : pool de connexions, variable d'env `DATABASE_URL`/Prisma, ou limite de connexions atteinte).
2. Vérifier les logs Vercel (function logs) pour l'erreur DB exacte.
3. Une fois le token Pushover configuré, réactiver l'alerte automatique pour que ce type d'incident soit notifié en temps réel au lieu d'être découvert a posteriori.

*(Rapport généré automatiquement par la routine restaumargin-deploy-guardian.)*
