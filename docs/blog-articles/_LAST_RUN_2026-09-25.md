Article prix-vente-menu-restaurant-methode publié le 2026-09-25. Composant BlogPrixVenteMenu.tsx créé, route ajoutée dans App.tsx, sitemap mis à jour.

---

## ⚠️ Run local parallèle (session desktop, même jour) — DOUBLON avec le run cloud

La routine a tourné **deux fois** le 25/09 : une fois dans le cloud à 07:14 UTC (commits `b579d90` + `70695b2`, ligne ci-dessus), une fois en local à 09:50 Paris. Les deux ont pris le même sujet, le slot #1 du backlog. Le run local n'a découvert le run cloud qu'au moment du `git pull`, parce que le run cloud n'avait pas mis à jour `backlog.md`. **Correctif à retenir** : chaque run doit faire `git pull` AVANT de choisir son sujet.

Décision : ne pas écraser ce qui est en ligne. Le run local s'est réaligné sur `origin/main` et a seulement ajouté :
- `prix-vente-menu-restaurant-methode.enrichi.md` : **2 971 mots**. La version publiée n'en fait que **1 346**, sous la cible de 2 000. Contenu en plus : coût pondéré, test de la pire combinaison, supplément, marge en euros, ventilation TVA 10/20 % des formules avec vin, affichage (arrêté du 27/03/1987), 5 FAQ. Les faits réglementaires ont été vérifiés en live. Le cas chiffré est déclaré « hypothèses illustratives ». Il n'y a aucune preuve sociale. **Action humaine** : reporter ce contenu dans `BlogPrixVenteMenu.tsx`, puis supprimer le `.enrichi.md`.
- `backlog.md` mis à jour : statut des pourboires → Publié (`bd301b3`) ; 5 nouveaux sujets (saisonnier, contrôle DDPP, affichages obligatoires, vente à emporter, assurances).
- Email Resend HTTP 200 (id `01a0d78e-fe28-700f-8b30-b794bd3f4edb`). L'utilisateur reçoit donc probablement deux emails aujourd'hui.

Autres constats :
- `BlogPrixVenteMenu.tsx` est absent de `BlogIndex.tsx` (0 occurrence). La page est orpheline de l'index `/blog`, alors qu'elle figure bien dans le sitemap.
- `BlogPrixCarte.tsx` affirme sans source que « 65-80 % des clients prennent la formule complète ». Selon le test de CLAUDE.md, cette phrase devrait être retirée ou sourcée.
- Aucun `_LAST_RUN` les 18 et 19/09 : run manqué, le troisième en un mois.
