# Health Report — 2026-04-10

**Heure de vérification :** 11:55 (heure locale)  
**Statut global :** ⚠️ AVERTISSEMENTS — Pas de panne critique, mais problèmes à surveiller

---

## ✅ Site principal

| URL | Statut |
|-----|--------|
| https://www.restaumargin.fr | ✅ OK — Page charge correctement |
| https://www.restaumargin.fr/login | ✅ OK — Page retourne du contenu (SPA React) |

---

## ⚠️ Problèmes détectés dans les logs Vercel

### 1. 404 récurrents sur `/api/presence/audit-log`

**Sévérité :** Modérée  
**Fréquence :** Toutes les ~60 secondes (polling permanent)

```
GET /api/presence/audit-log  → 404
```

Le client (frontend) appelle en boucle un endpoint qui n'existe pas. Cela génère des requêtes inutiles et peut indiquer un bug dans le composant de présence/audit.

**Action recommandée :** Vérifier le composant qui appelle `/api/presence/audit-log` — soit créer la route API, soit supprimer l'appel si l'endpoint a été renommé/supprimé.

---

### 2. 404 sur `/api/cron/health-check`

**Sévérité :** Faible  
**Occurrence :** Ponctuelle (observée 1 fois à 11:51:23)

```
GET /api/cron/health-check  → 404
```

Un cron job appelle une route qui n'existe pas encore.

**Action recommandée :** Créer la route `/api/cron/health-check` ou désactiver le cron dans Vercel.

---

### 3. Warnings SLOW sur plusieurs endpoints

**Sévérité :** Faible à modérée  
**Fréquence :** Récurrente (plusieurs fois par minute)

```
SLOW: GET /api/notifications  304
SLOW: GET /api/audit-logs     304
```

Ces endpoints répondent lentement de façon répétée. Cela peut affecter l'expérience utilisateur.

**Action recommandée :** Vérifier les requêtes Supabase derrière ces routes — ajouter de la mise en cache ou optimiser les requêtes SQL.

---

## ❌ Erreurs 500

Aucune erreur 500 détectée dans les 50 derniers logs. ✅

---

## Résumé

| Vérification | Résultat |
|---|---|
| Site principal | ✅ OK |
| Page login | ✅ OK |
| Erreurs 500 | ✅ Aucune |
| 404 récurrents | ⚠️ `/api/presence/audit-log` en boucle |
| Performance | ⚠️ SLOW sur notifications + audit-logs |
