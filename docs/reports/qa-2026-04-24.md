# QA Report — 2026-04-24

**Heure** : 20h00 Paris  
**Statut global** : ⚠️ RÉGRESSION DÉTECTÉE

---

## 1. Tests HTTP — Pages (15/15 ✅)

| Code | URL |
|------|-----|
| 200 | / |
| 200 | /pricing |
| 200 | /login |
| 200 | /demo |
| 200 | /a-propos |
| 200 | /blog |
| 200 | /blog/calcul-marge-restaurant |
| 200 | /outils/calculateur-food-cost |
| 200 | /temoignages |
| 200 | /station-produit |
| 200 | /mentions-legales |
| 200 | /cgv |
| 200 | /reactivation |
| 200 | /guide-marge/pizzeria |
| 200 | /guide-marge/brasserie |

**Pages OK : 15/15**

---

## 2. Tests API ⚠️ RÉGRESSIONS

| Attendu | Obtenu | Endpoint |
|---------|--------|----------|
| 200 | **503** | GET /api/health |
| 401 | **500** | POST /api/auth/login |

- `/api/health` retourne 503 — serveur API indisponible ou crashé
- `/api/auth/login` retourne 500 au lieu de 401 — erreur serveur sur authentification

---

## 3. Build TypeScript ❌ ÉCHEC

```
src/App.tsx(1,61): error TS2307: Cannot find module 'react' or its corresponding type declarations.
src/App.tsx(2,76): error TS2307: Cannot find module 'react-router-dom' or its corresponding type declarations.
src/App.tsx(3,487): error TS2307: Cannot find module 'lucide-react' or its corresponding type declarations.
src/App.tsx(133,51): error TS2503: Cannot find namespace 'React'.
src/App.tsx(138,12): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
```

Cause probable : `node_modules` manquants ou corrompus dans `client/`.

---

## 4. Synthèse

| Catégorie | Statut |
|-----------|--------|
| Pages HTTP (15/15) | ✅ OK |
| API /health | ❌ 503 |
| API /auth/login | ❌ 500 (attendu 401) |
| Build TypeScript | ❌ Échec (modules manquants) |

**Actions recommandées :**
1. Vérifier l'état du serveur API Node.js (pm2 status / logs)
2. Relancer `npm install` dans `client/` pour restaurer les modules TypeScript
3. Investiguer les logs backend pour l'erreur 500 sur /auth/login
