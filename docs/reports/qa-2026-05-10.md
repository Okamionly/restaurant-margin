# Rapport QA — 2026-05-10 20:00 (Paris)

## Résumé

| Métrique | Résultat |
|---|---|
| Pages HTTP OK | 15/15 ✅ |
| API health | 200 ✅ |
| API login (401 attendu) | 401 ✅ |
| Build TypeScript | ❌ FAIL (récurrent depuis 05-07) |

---

## Tests HTTP (15/15)

| Code | URL |
|---|---|
| 200 | / |
| 200 | /pricing |
| 200 | /login |
| 200 | /demo |
| 200 | /a-propos |
| 200 | /blog |
| 200 | /blog/calcul-marge-restaurant |
| 200 | /outils/calculateur-food-cost |
| 200 | /temoignages |
| 200 | /station-produit |
| 200 | /mentions-legales |
| 200 | /cgv |
| 200 | /reactivation |
| 200 | /guide-marge/pizzeria |
| 200 | /guide-marge/brasserie |

---

## Tests API

- `GET /api/health` → **200** ✅
- `POST /api/auth/login` (mauvais mdp) → **401** ✅

---

## Build TypeScript

**FAIL** — `node_modules` absent dans `client/` (dépendances non installées localement).

```
src/App.tsx(1,61): error TS2307: Cannot find module 'react' or its corresponding type declarations.
src/App.tsx(2,76): error TS2307: Cannot find module 'react-router-dom' or its corresponding type declarations.
src/App.tsx(3,497): error TS2307: Cannot find module 'lucide-react' or its corresponding type declarations.
src/App.tsx(180,51): error TS2503: Cannot find namespace 'React'.
src/App.tsx(185,12): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
```

**Cause :** `npm install` non exécuté dans `client/` — environnement local incomplet, pas une régression de code.  
**Statut :** Connu et non résolu depuis le 2026-05-07 (J+3).  
**Action requise :** `cd client && npm install` pour restaurer les types.

---

## Production

Toutes les 15 pages publiques répondent 200. Aucune régression de disponibilité détectée.  
Le build TypeScript reste en échec dans l'environnement local mais ne bloque pas la production.
