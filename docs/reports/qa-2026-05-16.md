# Rapport QA — 2026-05-16 20:00 Paris

## Résumé

| Catégorie | Statut |
|-----------|--------|
| Pages HTTP | ✅ 15/15 OK |
| API login (401) | ✅ OK |
| API health (200) | ✅ OK |
| Build TypeScript | ❌ FAIL (environnement) |

---

## 1. Tests HTTP — Pages (15/15)

| Code | URL |
|------|-----|
| 200 | / |
| 200 | /pricing |
| 200 | /login |
| 200 | /demo |
| 200 | /a-propos |
| 200 | /blog |
| 200 | /blog/calcul-marge-restaurant |
| 200 | /outils/calculateur-food-cost |
| 200 | /temoignages |
| 200 | /station-produit |
| 200 | /mentions-legales |
| 200 | /cgv |
| 200 | /reactivation |
| 200 | /guide-marge/pizzeria |
| 200 | /guide-marge/brasserie |

**Résultat : 15/15 pages retournent 200** ✅

---

## 2. Tests API

| Endpoint | Attendu | Reçu | Statut |
|----------|---------|------|--------|
| POST /api/auth/login (mauvais mdp) | 401 | 401 | ✅ |
| GET /api/health | 200 | 200 | ✅ |

---

## 3. Build TypeScript

**Statut : FAIL** ❌

```
src/App.tsx(1,61): error TS2307: Cannot find module 'react' or its corresponding type declarations.
src/App.tsx(2,76): error TS2307: Cannot find module 'react-router-dom' or its corresponding type declarations.
src/App.tsx(3,497): error TS2307: Cannot find module 'lucide-react' or its corresponding type declarations.
src/App.tsx(180,51): error TS2503: Cannot find namespace 'React'.
src/App.tsx(185,12): error TS7026: JSX element implicitly has type 'any' because no interface 'JSX.IntrinsicElements' exists.
```

**Cause identifiée** : `node_modules` absent dans l'environnement CI/container (ni `client/node_modules` ni `node_modules` racine). Il s'agit d'un problème d'environnement d'exécution, pas d'une régression de code. Le site en production fonctionne normalement (15/15 pages OK).

**Action recommandée** : Vérifier la configuration du pipeline CI pour s'assurer que `npm install` est exécuté avant `tsc --noEmit`.

---

## 4. SEO

- `/blog/calcul-marge-restaurant` — 200 ✅
- `/outils/calculateur-food-cost` — 200 ✅
- `/guide-marge/pizzeria` — 200 ✅
- `/guide-marge/brasserie` — 200 ✅

Toutes les pages SEO critiques sont accessibles.

---

## Conclusion

Site en production : **opérationnel**. Alerte émise pour le build TypeScript (manque de `node_modules` dans l'environnement de test).
