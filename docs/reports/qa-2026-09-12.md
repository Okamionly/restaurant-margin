# Rapport QA — 2026-09-12 18:27 Paris

## Résultat global : ⚠️ RÉGRESSION DÉTECTÉE

---

## 1. Tests HTTP — Pages (15/15 ✅)

| Code | URL |
|------|-----|
| 200 | / |
| 200 | /pricing |
| 200 | /login |
| 200 | /demo |
| 200 | /a-propos |
| 200 | /blog |
| 200 | /blog/calcul-marge-restaurant |
| 200 | /outils/calculateur-food-cost |
| 200 | /temoignages |
| 200 | /station-produit |
| 200 | /mentions-legales |
| 200 | /cgv |
| 200 | /reactivation |
| 200 | /guide-marge/pizzeria |
| 200 | /guide-marge/brasserie |

**Pages OK : 15/15** — Aucune régression HTTP.

---

## 2. Tests API

| Endpoint | Attendu | Obtenu | Statut |
|----------|---------|--------|--------|
| POST /api/auth/login (mauvais creds) | 401 | 401 | ✅ |
| GET /api/health | 200 | 200 | ✅ |

---

## 3. Build TypeScript — ❌ ÉCHEC

**Exit code : 2**

```
src/pages/BlogLogicielReservation.tsx(92,9): error TS2322:
Type '{ title: string; description: string; canonicalPath: string; }' is not assignable to type 'IntrinsicAttributes & SEOHeadProps'.
Property 'canonicalPath' does not exist on type 'IntrinsicAttributes & SEOHeadProps'.
```

**Fichier incriminé :** `client/src/pages/BlogLogicielReservation.tsx` ligne 92  
**Cause :** La prop `canonicalPath` est passée à `<SEOHead>` mais n'est pas définie dans l'interface `SEOHeadProps`.  
**Impact :** Build TypeScript bloqué — déploiement CI/CD potentiellement affecté.

---

## 4. Résumé

| Catégorie | Statut |
|-----------|--------|
| Pages HTTP (15/15) | ✅ OK |
| API login (401) | ✅ OK |
| API health (200) | ✅ OK |
| Build TypeScript | ❌ ÉCHEC |

**Action requise :** Ajouter `canonicalPath` dans `SEOHeadProps` ou supprimer la prop dans `BlogLogicielReservation.tsx`.
