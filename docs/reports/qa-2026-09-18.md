# Rapport QA — 2026-09-18

**Heure** : 20:00 (Paris)  
**Statut global** : ⚠️ RÉGRESSION DÉTECTÉE

---

## Tests HTTP — Pages (15 testées)

| Code | URL | Statut |
|------|-----|--------|
| 200 | / | ✅ |
| 200 | /pricing | ✅ |
| 200 | /login | ✅ |
| 200 | /demo | ✅ |
| 200 | /a-propos | ✅ |
| **403** | **/blog** | ❌ RÉGRESSION |
| 200 | /blog/calcul-marge-restaurant | ✅ |
| 200 | /outils/calculateur-food-cost | ✅ |
| 200 | /temoignages | ✅ |
| 200 | /station-produit | ✅ |
| 200 | /mentions-legales | ✅ |
| 200 | /cgv | ✅ |
| 200 | /reactivation | ✅ |
| 200 | /guide-marge/pizzeria | ✅ |
| 200 | /guide-marge/brasserie | ✅ |

**Pages OK : 14/15**

### Erreur détectée
- `/blog` → **403 Forbidden** (attendu : 200)
  - La page liste du blog retourne une erreur d'accès interdit
  - Les sous-pages du blog (ex: `/blog/calcul-marge-restaurant`) fonctionnent normalement
  - Possible cause : règle de redirection/auth mal configurée sur le serveur, ou CDN bloquant la racine `/blog`

---

## Tests API

| Endpoint | Code attendu | Code obtenu | Statut |
|----------|-------------|-------------|--------|
| GET /api/health | 200 | 200 | ✅ |
| POST /api/auth/login (wrong credentials) | 401 | 401 | ✅ |

---

## Build TypeScript

**Statut : ❌ ÉCHEC**

Erreurs détectées dans `client/src/App.tsx` :
```
src/App.tsx(1,61): error TS2307: Cannot find module 'react' or its corresponding type declarations.
src/App.tsx(2,76): error TS2307: Cannot find module 'react-router-dom' or its corresponding type declarations.
src/App.tsx(3,503): error TS2307: Cannot find module 'lucide-react' or its corresponding type declarations.
```

**Note** : Les erreurs semblent indiquer des modules manquants dans l'environnement de test CI (node_modules non installés). Il est probable que le code source soit correct mais que `npm install` n'ait pas été exécuté dans cet environnement. À vérifier en local.

Total : ~103 000 lignes d'erreurs (cascade due aux types React manquants).

---

## Résumé

| Catégorie | Résultat |
|-----------|----------|
| Pages HTTP | ⚠️ 14/15 (1 régression : /blog → 403) |
| API | ✅ OK |
| Build TypeScript | ❌ Échec (possiblement environnement CI) |

**Actions recommandées :**
1. Investiguer pourquoi `/blog` retourne 403 (vérifier config serveur/CDN/redirections)
2. Vérifier si le build TypeScript échoue aussi en local (`cd client && npm install && npx tsc --noEmit`)
