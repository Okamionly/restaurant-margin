# SEO Daily Push — 2026-07-04 (Samedi)

## Jour de rotation : SAMEDI (6) — Optimisation technique / Core Web Vitals

## Action réalisée
**Allègement du chunk `index` critique** — retrait des composants d'app-shell (overlays, bandeaux, modales) du bundle JavaScript téléchargé par **chaque page publique/SEO**.

### Constat (audit build)
- Architecture déjà bien code-split : toutes les pages en `lazyRetry`, `index.html` optimisé (Sentry/Crisp différés, fonts non-bloquantes, GA4 async, dns-prefetch/preconnect).
- **Vrai point noir** : le chunk `index` = **530 KB brut / 133,4 KB gzip**, chargé sur toutes les routes (landing, `/blog/*`, `/pricing`, comparatifs, niches) avant le premier rendu.
- Cause : 10 composants d'app-shell **rendus uniquement dans `AppLayout`** (routes authentifiées) mais **importés statiquement** → tout leur code polluait le chemin critique des pages SEO.
- Bonus : `CollaborationToast` importé mais **jamais rendu** (import mort).

### Correctif appliqué
- 10 composants convertis en `lazyRetry(() => import(...))` + `<Suspense fallback={null}>` (pop-in invisible, chrome uniquement) :
  `ConnectivityBar`, `OfflineSyncBar`, `WorkingIndicator`, `ContextualTooltips`, `FloatingActionBubble`, `MobileBottomNav`, `NPSModal`, `ShortcutsModal`, `SidebarLevelBadge`, `OnboardingProgress`.
- Même pattern que `CommandPalette` / `OnboardingWizard` déjà en place → comportement identique (même cycle de montage, juste chargé en chunks async hors chemin critique).
- Import mort `CollaborationToast` supprimé.

### Impact mesuré (`npm run build`)
| Chunk `index` | Avant | Après | Delta |
|---|---|---|---|
| Brut | 530 KB | 414 KB | **−116 KB (−22%)** |
| Gzip | 133,4 KB | 112,4 KB | **−21 KB (−15,7%)** |

10 chunks d'app-shell créés (17 KB gzip cumulés) — désormais chargés **seulement dans `AppLayout`** (jamais sur les pages publiques/SEO).

→ FCP / LCP / TBT réduits sur **toutes** les pages SEO indexées. Meilleur Core Web Vitals = signal de ranking Google.

## Fichiers modifiés
- `client/src/App.tsx` (imports statiques → lazy + wrappers Suspense sur les sites de rendu)

## Vérification
- **Build** : `tsc -b && vite build` OK, 0 erreur TS, `✓ built in 21.73s`.
- **Preview local** (dev server 5173), 0 erreur console introduite :
  - `/blog/calcul-marge-restaurant` → H1 OK, 21 × H2, JSON-LD présent, 32K chars.
  - `/pricing` → rendu OK (seul warning : `key` prop manquante dans `ComparisonTable` — **préexistant**, sans rapport, flaggé en tâche séparée).
  - `/` (homepage) → rendu OK, shaders WebGL actifs (2 canvas).
- **Production** (Googlebot) : `/`, `/blog/calcul-marge-restaurant`, `/pricing` → HTTP **200**, titles uniques servis (prerender OK).

## Commits pushés
- `f909a5d` — `perf(seo): lazy-load 10 overlays app-shell hors du chunk index critique`

## Vercel deploy
- `dpl_76onrTFrLkosbC84WidUfh9BuMMs` → **READY** (production, www.restaumargin.fr, build ~68s).

## Position Google
- Non re-vérifiée aujourd'hui (jour technique). Dernier point : #5 sur "marge restaurant" (audit du mercredi). Prochain audit positions : mercredi (rotation jour 3).

## Next action (Dimanche 5 — Review hebdo + EAT)
1. `reports/seo-weekly-2026-W27.md` : compter commits SEO de la semaine, comparer positions vs lundi, plan 7 actions.
2. Action EAT : enrichir 1 author bio (composant `BlogAuthor`) OU ajouter 1 mention presse crédible sur `/a-propos`.
3. À traiter hors-rotation si le temps : `ComparisonTable` key-prop warning (chip créé aujourd'hui).

## Note
SEO Daily Push 2026-07-04 OK — perf(index -21KB gzip / -15.7% sur le chemin critique de toutes les pages SEO) + 1 commit — Vercel READY.
