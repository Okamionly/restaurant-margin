# SEO Daily Push — 2026-07-13 (Lundi)

**Action de rotation (Lundi)** : nouvel article PAA sur une question non couverte.

## Réalisé
- **Nouvel article** : `BlogTauxMarque` → `/blog/taux-de-marque-taux-de-marge`
  - **Question PAA ciblée** : « Taux de marque ou taux de marge : quelle différence ? » — grep confirmé **zéro page existante** sur « taux de marque » (gap réel, confusion comptable classique FR, cœur requête marge).
  - **~3 460 mots**, 10 sections H2, featured snippet 40-60 mots en intro, tableau de conversion coefficient/marge/marque/food-cost, encadrés formules, 5 erreurs de pricing, méthode 7 étapes.
  - **Schemas** : FAQPage (14 Q/A) + HowTo (7 étapes) + Article + BreadcrumbList, émis en un seul `@graph` (pattern SEOHead corrigé le 07-10).
  - **Maillage interne fort** (priorité audit #1) : liens contextuels vers hub `calcul-marge-restaurant`, `coefficient-multiplicateur`, `prime-cost`, `prix-de-vente`, `reduire-food-cost`, `5-ratios-cles`, `marge-beneficiaire`, `marge-boissons`, `seuil-rentabilite` + bloc « Pour aller plus loin » (8 liens) + CTA calculateur/logiciel.

## Files modified
- `client/src/pages/BlogTauxMarque.tsx` (**nouveau**, 906 lignes)
- `client/src/App.tsx` (lazy import `BlogTauxMarque` + route `/blog/taux-de-marque-taux-de-marge`)
- `client/src/pages/BlogIndex.tsx` (entrée listing en tête — évite page orpheline)
- `client/public/sitemap.xml` (URL, priority **0.85**, lastmod 2026-07-13)
- `client/scripts/prerender.cjs` (entrée ROUTES title + description, type article)
- `reports/seo-daily-2026-07-13.md` (ce fichier)

## Commits pushed
- `3602b20` — seo(paa-daily): nouveau BlogTauxMarque cible question PAA 'taux de marque ou taux de marge' (5 files, +928)

## Vérifs (toutes OK)
- **Build** : `vite build` **OK** (12,6 s, chunk `BlogTauxMarque-*.js` 52 kB) + `tsc -b` **exit 0** (0 erreur TS).
- **Prerender** : route générée, `<title>` / `<meta description>` / `<link canonical>` / H1 correctement bakés (pas le title générique).
- **Rendu navigateur** (dev :5173) : page rendue intégralement (featured snippet, formules, sommaire 11 items, sections) — **0 erreur console**. (Screenshot timeout dû au shader WebGL ; contenu validé via extraction texte.)
- **Deploy Vercel** : **live en ~75 s** après push. Googlebot voit le bon `<title>` + `<meta description>` sur `/blog/taux-de-marque-taux-de-marge` (HTTP 200).
- **Sitemap prod** : contient la nouvelle URL (lastmod 2026-07-13).
- **JSX** : aucun caractère `>`/`<` problématique dans le texte (grep clean, rule #9).
- `LanguageToggle.tsx` non touché (rule #5). `aggregateRating` factice : toujours absent.

## Structured data (note architecture)
Le HTML prérendu servi aux crawlers ne contient QUE le `BreadcrumbList` (baké par `prerender.cjs`, lignes 861-895). Le `@graph` FAQPage+HowTo+Article est injecté côté client par react-helmet à l'hydratation → Google (qui exécute le JS) le récupère au rendu. **C'est le comportement identique aux ~50 autres pages blog**, pas un bug. RAS.

## ⚠️ Toujours à traiter (propriétaire) — inchangé depuis 07-12
**IndexNow** : Yandex **HTTP 202** (accepté → réseau IndexNow re-diffuse à Bing). Bing + api.indexnow.org **403 `UserForbiddedToAccessSite`** (2 essais), **malgré le fichier clé servi correctement en prod** (`2705a97b…184a951.txt`, HTTP 200, contenu == clé, présent dans `dist/`). Ce n'est pas un problème de deploy ni de code → **état de vérification côté Bing**. Action suggérée : **claim/vérifier restaumargin.fr dans Bing Webmaster Tools** (et y (re)déclarer la clé IndexNow) pour débloquer l'acceptation directe Bing. Non résolvable en autonome (write sur compte externe).

## Next action (Mar 07-14 — Mardi : boost article existant faible rank)
Cibler un article blog court non optimisé à fond (< 800 lignes) : +500-1000 mots, section « cas concret chiffré », FAQ supplémentaire, 5+ internal links, `dateModified` → 2026-07-14. Candidats : pages informationnelles absentes du top 10 selon audit (food-cost, menu-engineering, rentabilité). Vérifier au passage le rendu du `@graph` Article sur 2-3 pages via Rich Results Test.
