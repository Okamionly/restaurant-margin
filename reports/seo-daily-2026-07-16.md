# SEO Daily Push — 2026-07-16 (Jeudi)

**Action de rotation (Jeudi)** : nouvelle page comparatif ou niche métier.

## Réalisé

### 1. Nouvelle page niche — Restaurant indien (action de rotation)
- **Nouvelle page** : `LogicielMargeIndien` → `/logiciel-marge-restaurant-indien`
  - **Niche choisie** : restaurant indien. Vérifié **non couvert** (grep `prerender=0`, aucune route dédiée ; les autres pistes proches étaient déjà prises → cannibalisation évitée : « brasserie » couvert par `logiciel-marge-bistrot` + `logiciel-marge-cafe-bar` + `guide-marge/brasserie`, « japonais » par la page sushi).
  - **~3 200 mots**, 10 sections H2 + FAQ, featured snippet 40-60 mots en intro (hero subtitle + reperes).
  - **Angle unique** : la **sauce de base (gravy)** standardisée comme levier marge #1 (0,45-0,70 €/portion vs 0,90-1,50 € à la commande) — spécificité réelle de la cuisine indienne, différencie la page des autres niches.
  - **Structure marge** : staples ultra-rentables (naan 7 %, riz 11 %, dal 10 %, chana 11 %) vs protéines chères (agneau 30 %, gambas 32 %) — miroir de la page italien.
  - **Contenu chiffré** : tableau 10 plats (cm/pv/food cost/marge brute), décomposition poulet tikka masala (standard vs premium), table marge boissons (bière indienne x5-6, lassi, chai), menu mix cible, 6 KPI, cas concret Lyon 8 %→19 % en 8 mois (4 phases).
  - **Mots-clés cibles** : « logiciel marge restaurant indien », « food cost restaurant indien », « marge restaurant indien », « coefficient restaurant indien », « cout curry / biryani / naan », « rentabilité restaurant indien ».
  - **Schemas** : FAQPage (10 Q/A) + Article + SoftwareApplication (29 €/mois, sans aggregateRating) + BreadcrumbList.
  - **Maillage interne fort** (priorité audit #1) : liens contextuels vers hub `logiciel-marge-restaurant`, `logiciel-marge-restaurant-italien` (sibling), `blog/menu-engineering-boston-matrix`, `blog/reduire-food-cost`, `blog/coefficient-multiplicateur`, `blog/marge-boissons-restaurant`, `outils/calculateur-food-cost` + bloc « Pour aller plus loin » (4 liens) + CTA. Ajoutée à la liste de maillage `guide` de `prerender.cjs` (rétro-liens depuis toutes les autres pages).

### 2. Fix critique — retrait des faux avis aggregateRating (priorité audit #4)
- **24 blocs `aggregateRating` fabriqués supprimés sur 23 pages** (valeurs inventées 4.8/47, 4.8/127, 4.8/150 injectées dans les schemas SoftwareApplication/Product).
- **Pourquoi** : RestauMargin n'a **pas d'avis agrégés réels** à citer. Ces faux avis en données structurées exposent le site à une **action manuelle Google (structured data spam / faux avis)** qui plomberait **tout** le ranking — annulant l'effet de tout le daily push. L'audit l'a explicitement flaggé ⚠️ « NE JAMAIS ajouter d'aggregateRating ». Le fichier `client/index.html` était déjà nettoyé (run précédent), mais 23 pages `.tsx` le portaient encore.
- Suppression **surgicale** (script Node 2 passes regex) : seul le bloc `aggregateRating` retiré, le reste des schemas (offers, featureList, description) intact. **0 résidu** vérifié repo-wide, build OK.
- Pages touchées : `Landing`, `CalculateurMargeRestaurant`, `ComparatifRestauMarginZenchef` (×2), `Alternative{Zenchef,Lightspeed,TheFork,LAddition}`, `BlogLogiciel{Caisse,Gestion}`, et 13 pages `LogicielMarge*` (Bistrot, Boulangerie, Burger, CafeBar, Creperie, DarkKitchen, FoodTruck, Glacier, Kebab, Pizzeria, Restaurant, SaladBar, Sushi, Traiteur).

## Files modified
- `client/src/pages/LogicielMargeIndien.tsx` (**nouveau**, ~850 lignes)
- `client/src/App.tsx` (lazy import `LogicielMargeIndien` + route `/logiciel-marge-restaurant-indien`)
- `client/public/sitemap.xml` (URL, priority **0.85**, lastmod 2026-07-16)
- `client/scripts/prerender.cjs` (entrée ROUTES title + description + entrée maillage `guide`)
- 23 pages `.tsx` (retrait aggregateRating — voir liste ci-dessus)
- `reports/seo-daily-2026-07-16.md` (ce fichier)

## Commits pushed
- `20b3471` — seo(niche): nouvelle page LogicielMargeIndien (restaurant indien)
- `a21b167` — seo(fix): retrait aggregateRating fabrique sur 23 pages (risque action manuelle Google)

## Vérifs
- **Build** : `vite build` **OK** (17,7 s, chunk `LogicielMargeIndien-BZB3Vncx.js` généré, « files generated »). Les 24 retraits de schema compilent proprement (0 erreur syntaxe JS).
- **JSX** : aucun caractère `>` / `<` problématique dans le texte JSX (le seul `> 33 %` était dans un string literal, remplacé par « superieur a » par précaution, rule #8/#9).
- **`LanguageToggle.tsx`** non touché (rule #5). **Pas de `--force`** (rule #7).
- **Deploy Vercel** : commit `a21b167` **live en ~44 s** après push (polling prod confirmé). Googlebot voit le bon `<title>` (`Logiciel marge restaurant indien | Food cost curry | RestauMargin`) + bonne `<meta description>` sur `/logiciel-marge-restaurant-indien` (HTTP 200, pas le title générique).
- **Sitemap prod** : nouvelle URL **présente** (vérifié, lastmod 2026-07-16).
- **Contenu prod** : page rend le contenu réel (« restaurant indien » et « food cost » présents de multiples fois dans le HTML servi).
- **Schemas prod** : `BreadcrumbList` + `FAQPage` + `SoftwareApplication` **tous les 3 bakés dans le HTML statique** (vérifié via curl sans JS) — meilleur que prévu (les 3 sont prérendus, pas seulement le breadcrumb).

## Deploy & indexation
- **Vercel** : **READY** — déploiement `a21b167` live ~44 s après push, page servie correctement (title + meta + contenu + 3 schemas). Aucun ERROR.
- **IndexNow** (108 URLs) : **Yandex HTTP 202 Accepted** ✓ (→ le réseau IndexNow re-diffuse à Bing). **Bing + api.indexnow.org HTTP 403 `UserForbiddedToAccessSite`** — **malgré le fichier clé servi correctement en prod** (`2705a97b…184a951.txt`, HTTP 200, `text/plain`, contenu == clé, match vérifié aujourd'hui). Ce n'est PAS un problème de deploy/code → **état de vérification côté Bing** (identique aux runs 07-12 et 07-13). **Action propriétaire suggérée** : claim/vérifier `restaumargin.fr` dans **Bing Webmaster Tools** et y (re)déclarer la clé IndexNow. Non résolvable en autonome (write sur compte externe).

## Note structured data
Cette page sert **les 3 schemas (BreadcrumbList + FAQPage + SoftwareApplication) directement dans le HTML statique** (vérifié via curl sans exécution JS), donc lisibles par tous les crawlers sans rendu. Le schema `Article` complète le `@graph` côté client à l'hydratation. RAS — au contraire, meilleure couverture structured-data que supposé.

## Next action (Ven 07-17 — Vendredi : sitemap fresh + ping + rich snippets)
Mettre `lastmod` du jour sur les 10 pages les plus importantes du sitemap, vérifier la version prod, tester 5 pages clés en Googlebot (titles corrects), et **valider via Rich Results Test que le retrait d'aggregateRating est bien pris en compte** (aucune page ne doit plus déclarer de Review/Rating). Re-tenter IndexNow après le déploiement de ce jour.
