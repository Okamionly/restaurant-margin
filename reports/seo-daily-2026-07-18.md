# SEO Daily Push — 2026-07-18 (Samedi)

**Rotation du jour : SAMEDI = Optimisation technique (Core Web Vitals / réduction bundle).**

## Action réalisée

Sortie de **Login** (et de ses dépendances i18n) du chunk `index` critique que **toutes** les pages publiques/SEO téléchargent avant le first paint.

### Diagnostic
- `client/src/App.tsx` importait `Login` **eagerly** → bundlé dans le chunk `index` (chargé sur chaque page landing/blog/pricing/outils), alors que Login ne s'affiche que sur `/login`.
- Login tirait avec lui le hook **`useTranslation` + la locale `fr` bundlée (~99 kB)** et un composant **`PaperFoldBackground` WebGL**.
- **Aucune page SEO n'importe `useTranslation`** (vérifié : 0 référence dans `Landing`, `BlogCalcMarge`, `BlogCoefficient`, `Pricing`, `FoodCostCalculator`, `NicheLanding`, `BlogFoodCost`). Ces pages téléchargeaient donc ~99 kB de machinerie i18n + locale **jamais utilisée**.
- `client/src/main.tsx` faisait un **prefetch idle du Dashboard** (chunk auth-gated) inconditionnel → visiteurs anonymes et crawlers téléchargeaient du code authentifié sur les pages SEO.

### Changements
- `App.tsx` : `Login` → `lazyRetry(() => import('./pages/Login'))` + route `/login` enveloppée dans `<Suspense>` (même pattern que les 40+ autres routes publiques du fichier).
- `main.tsx` : prefetch Dashboard **gate sur `localStorage.getItem('token')`** → skip pour les visiteurs anonymes / crawlers.

## Mesure (preuve, pas estimation)

Chunk `index` (chargé sur **chaque** page SEO), sortie `vite build` :

| | Avant | Après | Delta |
|---|---|---|---|
| `index` raw | 282.50 kB | 161.14 kB | **−121 kB** |
| `index` gzip | 75.74 kB | **40.95 kB** | **−34.8 kB (−46%)** |

`useTranslation` (98.8 kB) et `Login` (16.1 kB) + `PaperFoldBackground` (4.8 kB) sont désormais des chunks séparés qui ne chargent **que** sur les pages qui les importent. Aucun octet perdu (161.14 + 98.8 + 16.1 + 4.8 ≈ 282.5).

**Vérif runtime (prod-preview `vite preview`, Performance API) sur `/blog/calcul-marge-restaurant` :**
- `useTranslation` chargé : **non**
- `Login` chargé : **non**
- `PaperFoldBackground` chargé : **non**
- `index` servi : `index-DyCD5f00.js` (slim)
- Page rendue correctement (h1 « Marge restaurant 2026 : calcul, formule et guide complet »)

**Non-régression `/login`** : rendu vérifié dans le navigateur (email + mot de passe + « Se connecter »), lazy Login résolu via Suspense.

## Impact SEO attendu
Chaque page publique/SEO (landing, `/blog/*`, `/pricing`, `/outils/*`, niches) ship **~35 kB gzip de JS critique en moins** avant interactivité → améliore FCP / LCP / TBT, signaux Core Web Vitals que Google pondère dans le ranking mobile.

## Files modified
- `client/src/App.tsx`
- `client/src/main.tsx`

## Commits pushed
- `a4e539d` — perf(seo): sort Login (+ i18n) du chunk index critique -> -46% JS sur pages SEO

## Vercel deploy
Push `b9f3c52 → a4e539d` sur `main`. Webhook Vercel opérationnel (dernier deploy `b9f3c52` = READY). Deploy `a4e539d` déclenché — statut vérifié en fin de run.

## IndexNow
**Non applicable aujourd'hui** : changement code/perf uniquement, **aucune modification de contenu ni de sitemap**. Les URLs crawlables et leur contenu sont identiques → pinger IndexNow re-soumettrait des URLs inchangées (bruit). Sera relancé au prochain run avec changement de contenu (lundi = nouvel article PAA).

## Next action (Dimanche 2026-07-19)
Review hebdo + plan semaine + EAT :
- Compter les commits SEO de la semaine, comparer positions Google vs lundi.
- `reports/seo-weekly-2026-W29.md`.
- Action EAT : enrichir 1 author bio détaillée sur 1 article (composant BlogAuthor) OU mention presse sur `/a-propos`.
