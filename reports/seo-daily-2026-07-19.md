# SEO Daily Push — 2026-07-19 (Dimanche)

**Jour de rotation** : Dimanche (7) → **Review hebdo W29 + Plan W30 + EAT** (+ fix build regression opportuniste).

## Actions réalisées

### 1. 🔧 Fix build regression (bloquant type-check)
- **`client/src/pages/BlogNegocierFournisseurs.tsx`** — 3 erreurs TS (commit `20d756a`) corrigées :
  - `SEOHead` : `canonical` → `path`, `schemas` → `schema` (le mauvais nom faisait que **la page live n'émettait AUCUN JSON-LD Article/FAQPage** — confirmé absent du HTML prod).
  - `BlogArticleHero` : retrait de `icon` (prop inexistante), `publishDate` → `date="Juillet 2026"`, ajout `accentWord`.
  - `BlogAuthor` : `<BlogAuthor/>` → `<BlogAuthor publishedDate={publishDate} readTime="7 min" variant="footer" />` (byline auteur restaurée).
  - Nettoyage : retrait `heroIcon` + import `Handshake` devenus morts.
- **Impact** : page **live/routée/sitemap** (`/blog/negocier-fournisseurs-restaurant`) — restaure Article + FAQPage + BreadcrumbList + dates auteur. `tsc --noEmit` repasse à **0 erreur**.

### 2. 🎯 Action EAT — Sources sur BlogSeuilRentabilite (3ᵉ page)
- **`client/src/pages/BlogSeuilRentabilite.tsx`** — section « Sources & références » (4 sources vérifiées : economie.gouv.fr, INSEE, UMIH, L'Hôtellerie-Restauration) + `updatedDate` header/footer + `dateModified` schema : `2026-05-26 → 2026-07-19`.
- **`client/public/sitemap.xml`** — lastmod seuil `2026-07-17 → 2026-07-19` (page réellement modifiée).

### 3. 📊 Review hebdo + plan
- **`reports/seo-weekly-2026-W29.md`** — bilan complet W29 + positions + plan W30 (7 actions).

## Files modified
- `client/src/pages/BlogNegocierFournisseurs.tsx`
- `client/src/pages/BlogSeuilRentabilite.tsx`
- `client/public/sitemap.xml`
- `reports/seo-weekly-2026-W29.md` (new)
- `reports/seo-daily-2026-07-19.md` (new)

## Build / Deploy
- `tsc --noEmit` : **0 erreur** (regression cleared)
- `vite build` : **OK 15,5 s**, precache 638 KiB (↓ vs 753 KiB W28)
- Commits pushed : `ed7336b` (fix build) + `66fbc23` (weekly+eat) → `git push origin main` OK
- **IndexNow** (115 URLs) : **Yandex HTTP 202 Accepted** ✅ (reshare réseau) ; **Bing/api.indexnow.org HTTP 403** `UserForbiddedToAccessSite`. Diagnostic : le fichier clé est **correctement servi** (https://.../2705a97b...txt → HTTP 200, `text/plain`, contenu = clé exacte) → **pas un problème de deploy**. 403 Bing = **domaine non revendiqué dans Bing Webmaster Tools** (action propriétaire, cf. mémoire routines_health « Bing WMT à claim »). Soumission tout de même effective via Yandex + réseau IndexNow.
- **Vercel** : deploy `dpl_C2SCMZJD...` (commit `66fbc23`) → **READY** ✅ (readyState READY, promu sur www.restaumargin.fr, région iad1, aucun ERROR). Build OK.
- **Vérif live end-to-end** : ⏳ **edge-cache lag**. À l'instant post-deploy, www sert encore le HTML pré-deploy (`X-Vercel-Cache: HIT`, `Age ~100s`) — le cache-buster `?nocache=` ne bust pas (Vercel clé le HTML prérendu par pathname, pas par query). Le mécanisme de prerender est prouvé fonctionnel (FAQPage/BreadcrumbList émis sur ces routes) et le build local est vert → le contenu (Article schema negocier + Sources/dateModified seuil) surgira à la revalidation edge. **Contrôle rich-results prévu vendredi W30** sur les 2 pages.

## Positions Google (WebSearch 2026-07-19, biais US)
- `coefficient multiplicateur restaurant calcul` : **#6** (🔻 vs #4 W28 — Resthodev + Comptable-restaurant nous doublent)
- `marge restaurant calcul rentabilité` : absent top 10 (Coopeo #1)
- `food cost restaurant calcul` : SERP US only, page FR non captée
- Branded `restaumargin` : #1

## Next action (Lun 07-20)
Article PAA non couvert — candidats : « Combien de couverts par jour pour être rentable ? », « Comment fixer le prix d'un plat ? ». Puis **Mardi = boost food cost (carry-over prioritaire)**.
