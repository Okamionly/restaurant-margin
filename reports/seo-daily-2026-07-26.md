# SEO Daily Push — 2026-07-26 (Dimanche)

**Jour de rotation** : Dimanche (7) → **Review hebdo W30 + Plan W31 + signal EAT**.

## Actions réalisées

### 1. 🔧 Réparation critique (hors rotation, priorité) — article publié samedi invisible pour Google

L'audit `comm -23 sitemap prerender` (routine de chaque run, cf. `lessons.md` 29/05) a levé une page. Vérification en prod puis `tsc` → **4 défauts simultanés** sur `/blog/reprise-restaurant-guide-acheteur` (`827997c`, blog-writer, samedi) :

| Défaut | Effet | Fix |
|---|---|---|
| Absent de `prerender.cjs` | Googlebot recevait le **title de la homepage** (constaté live) | entrée ajoutée → 115 fichiers prerendus |
| `canonical=` (prop inexistante) | aucune balise canonical | → `path=` |
| `jsonLd=` (prop inexistante) | **aucun JSON-LD** (Article + FAQPage + BreadcrumbList perdus) | → `schema=` |
| `<BlogAuthor />` sans props requises | `new Date(undefined)` → **« Invalid Date » visible** | props + header/footer |

Récidive exacte de W29 (`BlogNegocierFournisseurs`). Cause racine : `build:vercel` = `vite build` **sans `tsc`**.

### 2. Signal EAT — attribution d'auteur (véridique, zéro fabrication)

Audit des 46 articles (schema `dateModified` vs byline visible) : **22 cohérents, 4 sans aucun `<BlogAuthor>`, 19 sans date de MAJ visible, 1 drift**.

- **4 articles dotés de la signature** (header + footer) : `BlogChiffreAffaires`, `BlogCoutRevient`, `BlogTauxOccupation`, et `BlogPsychologiePrix` (dont la signature en **texte brut** devient le composant structuré : `<time itemProp>`, schema `Organization`, lien `/a-propos`).
- **Drift `BlogFoodCost` résolu** : `git log` prouve que la dernière modif réelle date du **2026-06-03** → le schema disait vrai, la byline (`2026-05-26`) était périmée. Alignée. Hero « Mai » → « Juin ». **Aucun bump à la date du jour** (fraîcheur non méritée = faux signal).

> Écarté pour la 4ᵉ semaine : auteur/LinkedIn fictif et mention presse fictive. Même classe de faux signal que l'`aggregateRating` retiré en W28-W29.

### 3. Review hebdo W30 → `reports/seo-weekly-2026-W30.md`

## Files modified
- `client/src/pages/BlogRepriseRestaurant.tsx`
- `client/scripts/prerender.cjs`
- `client/src/pages/BlogChiffreAffaires.tsx`, `BlogCoutRevient.tsx`, `BlogPsychologiePrix.tsx`, `BlogTauxOccupation.tsx`
- `client/src/pages/BlogFoodCost.tsx`
- `reports/seo-weekly-2026-W30.md` (new), `reports/seo-daily-2026-07-26.md` (new)

## Commits pushed
- `05ad494` — `fix(seo): repare BlogRepriseRestaurant — props SEOHead invalides + prerender manquant`
- `e081c67` — `seo(weekly+eat): attribution auteur sur 4 articles + drift dateModified + review W30`
- push `dbe810b..e081c67` OK

## Build / vérifications
- **`npx tsc --noEmit`** : **2 erreurs → 0** (les 2 étaient dans l'article de samedi).
- **`vite build`** : `✓ built in 14.62s`, precache 639,97 KiB.
- **`node scripts/prerender.cjs`** : 115 fichiers (114 avant). Title vérifié dans `dist/blog/reprise-restaurant-guide-acheteur/index.html` → `Reprise de restaurant : comment eviter les pieges — Guide acheteur 2026 | RestauMargin` + canonical correct.
- **Vérif runtime (dev-server, DOM)** sur les 5 pages modifiées : **2 blocs auteur** chacune, **0 « Invalid Date »**, `<time itemProp="datePublished">` correct, `dateModified` visible et **égal au schema** sur FoodCost (`2026-06-03`).
- **⚠️ Piège local reconfirmé** : title générique + aucun `@graph` en dev. **Test de contrôle** sur `/blog/calcul-marge-restaurant` (page saine en prod) → **mêmes symptômes** ⇒ helmet inerte dans l'environnement local (Service Worker PWA, cf. `lessons.md` 12/07), **pas** une régression. Googlebot n'utilise pas de SW.
- **Screenshot indisponible** (run planifié, pane navigateur non affiché) — vérification faite par assertions DOM.

## ✅ Deploy Vercel — propagé et vérifié en prod

Les 2 deploys sont restés **`QUEUED` ~12 min** (file d'attente, concurrence Hobby — aucun `ERROR`) puis sont passés. **Vérifié en prod après propagation** :

| Défaut | Vérification prod | Statut |
|---|---|---|
| Title homepage servi à Googlebot | `curl -A Googlebot` → `<title>Reprise de restaurant : comment eviter les pieges — Guide acheteur 2026 \| RestauMargin</title>` | ✅ **corrigé** |
| Canonical absent | `<link rel="canonical" href=".../blog/reprise-restaurant-guide-acheteur">` | ✅ **corrigé** |
| « Invalid Date » visible | DOM prod : `invalidDate: false`, `authorBlocks: 2`, `h1` correct | ✅ **corrigé** |
| JSON-LD Article/FAQ/Breadcrumb | voir ci-dessous | ⚠️ **non confirmé de bout en bout** |

### ⚠️ Question ouverte (préexistante, PAS une régression du jour) — le JSON-LD `Article` est-il vu par Google ?

Le prop est désormais correct (`schema=`, validé par `tsc` : les schemas sont réellement passés à `SEOHead` au lieu d'être silencieusement ignorés comme props inconnues). Mais je **n'ai pas pu prouver** qu'ils atteignent le DOM en prod :

- Le HTML statique prérendu ne contient **pas** de `"@type":"Article"` — attendu, `prerender.cjs` n'injecte que title/meta/OG/canonical, pas le JSON-LD (rendu par React côté client).
- Test navigateur non concluant : la **PWA ré-enregistre son Service Worker à chaque chargement** (`swActive: true` après désinscription) → le shell caché est resservi, helmet paraît inerte. Impossible de trancher dans ce navigateur.
- **Constat qui élargit le sujet** : `Article=0` dans le HTML statique de **toutes** les pages testées, y compris `/blog/coefficient-multiplicateur` (qui est **#1 sur sa requête**) et `/blog/calcul-marge-restaurant`. Ce n'est donc pas propre à l'article réparé aujourd'hui — c'est l'état de tout le blog.

**Ce qu'il faut pour trancher** (action propriétaire, non automatisable) : passer 1 URL dans le **test des résultats enrichis Google** ou l'**inspection d'URL Search Console** — seuls outils qui exécutent le JS comme Googlebot, sans Service Worker. Si `Article` n'y apparaît pas, **~46 articles n'ont aucune donnée structurée Article** (perte E-E-A-T + freshness), ce qui devient le chantier SEO n°1. À NE PAS traiter comme acquis dans un sens ou dans l'autre avant ce test.

## IndexNow (117 URLs)
- `yandex.com` → **HTTP 202 Accepted** ✅
- `api.indexnow.org` + `www.bing.com` → **HTTP 403** `UserForbiddedToAccessSite` (inchangé depuis 07-19).

## Positions Google (WebSearch 2026-07-26)
| Query | Position | Δ vs 07-19 |
|---|---|---|
| `coefficient multiplicateur restaurant calcul` | **🏆 #1** | 🔼 **#6 → #1** |
| `marge restaurant calcul rentabilité` | absent top 8 | = |
| `calcul food cost restaurant formule ratio matière` | absent top 9 | = |

Restopilot recule #1 → #5 sur coefficient ; les 2 experts-comptables qui nous avaient doublés en W29 passent #4→#7 et #5→#8.

## Blockers à remonter au propriétaire
1. 🔴 **Aucun agent n'a tourné du 20 au 23/07** (4 jours vides, tous agents) → 5 des 7 actions W30 jamais exécutées. Problème d'ordonnanceur.
2. 🔴 **`tsc` absent du build Vercel** → 2 articles publiés cassés en 2 semaines. Ajouter `tsc --noEmit` au `buildCommand` ou en pre-commit.
3. 🟠 **Bing IndexNow 403** → revendiquer `restaumargin.fr` dans Bing Webmaster Tools (login user requis).

## Next action (Lundi 27/07)
Rotation Lundi (1) = **nouvel article PAA**. Croiser l'existant (`ls client/src/pages/Blog*.tsx` + backlog) avant d'écrire — piège doublon documenté.
