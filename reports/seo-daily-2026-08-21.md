# SEO Daily Push — vendredi 21 août 2026

**Rotation** : jour 5 → sitemap fresh + ping IndexNow + audit résultats enrichis.

## Contexte au démarrage

`git pull` a ramené 10 commits du jour (blog-writer, cto, coo, ceo, cmo, outreach, qa —
un autre système multi-agent tourne sur ce repo). Le plus pertinent pour le SEO :
`cca816b feat(blog): nouvel article google-my-business-restaurant` avec une route
ajoutée dans `App.tsx`.

## Actions réalisées

### 1. Sitemap fresh — 10 pages prioritaires
`lastmod` mis à `2026-08-21` sur : `/`, `/pricing`, `/launch`, `/logiciel-marge-restaurant`,
`/blog/calcul-marge-restaurant` (hub), `/outils/calculateur-marge-restaurant`,
`/comparatif-logiciels-restaurant`, `/comparatif-restaumargin-vs-zenchef`,
`/glossaire-restauration`, `/blog/faq-marge-restaurant-25-questions`.

### 2. Fix — article orphelin `google-my-business-restaurant`
La page publiée aujourd'hui avait une route React mais :
- **absente de `prerender.cjs`** → Googlebot recevait le title générique de la
  homepage à la place du sien (vérifié par `curl -A Googlebot`, réparé, re-vérifié
  : title correct en prod).
- **absente de `BlogIndex.tsx`** → orpheline du maillage interne blog.

Correctif appliqué, testé (`tsc --noEmit` clean, `vite build` OK), poussé, vérifié
en prod. **Doublon découvert après coup** : blog-writer avait déjà son propre append
sitemap (priority 0.7, section chronologique) que mon premier check avait raté —
dédupliqué dans un 2ᵉ commit. `check-seo-coverage.cjs` confirme désormais
**120 URLs sitemap = 120 prerendues, 0 doublon**.

### 3. Audit résultats enrichis — question ouverte de mémoire résolue
`curl -A Googlebot` sur le HTML statique ne montre **aucun schema `Article`**
(seulement Organization/WebSite/SoftwareApplication/BreadcrumbList injectés
globalement) — confirmant que le JSON-LD `Article`/`FAQPage` de `SEOHead` est
rendu côté client React uniquement.

**Mais** le test officiel Google (`search.google.com/test/rich-results`, qui exécute
le JS comme le vrai Googlebot) sur `/blog/calcul-marge-restaurant` donne :
**7 éléments valides** — Articles (1 valide), Fils d'Ariane (2), Organisation (2),
Applications logicielles (1), Vidéos (1). **Le schema Article est donc bien vu et
accepté par Google en production**, malgré son absence du HTML brut. Ça referme la
question ouverte du 26/07 dans `project-restaumargin-blog` (le doute venait d'un test
navigateur pollué par le Service Worker — le test hors-navigateur est concluant).
2ᵉ test sur le nouvel article bloqué par une demande de connexion Google
(rate-limit anonyme) — pas de login tenté.

### 4. Titles Googlebot — 5 pages clés
Toutes correctes : homepage, hub calcul-marge, pricing, nouvel article (post-fix),
comparatif Zenchef.

## Fichiers modifiés

- `client/public/sitemap.xml` — 10 lastmod + 1 ajout puis dédup
- `client/scripts/prerender.cjs` — +1 entrée `google-my-business-restaurant`
- `client/src/pages/BlogIndex.tsx` — +1 carte blog

## Commits

- `3f5e1a0` — seo(freshness): sitemap update + fix orphan article google-my-business-restaurant
- `1c5fc04` — seo(fix): dedup entree sitemap google-my-business-restaurant

## Vérifications

| Contrôle | Résultat |
|---|---|
| `npx tsc --noEmit` | exit 0 |
| `npx vite build` ×2 | OK, files generated |
| `check-seo-coverage.cjs` | 120 sitemap = 120 prerendues |
| Push origin/main | OK (2 commits) |
| Déploiement prod | vérifié par contenu (sitemap + titles Googlebot live), pas par l'API — **Vercel MCP toujours 403 Forbidden sur `list_deployments`**, connu depuis le 19/08 |
| IndexNow | Yandex `200 OK` · api.indexnow.org + Bing `403 UserForbiddedToAccessSite` — clé bien servie (HTTP 200 vérifié), cause = **site non claim Bing Webmaster Tools**, pas un problème de déploiement. Connu depuis le 19/08, toujours pas traité. |
| Rich Results Test (hub) | 7 éléments valides dont Article |

## Positions Google

Non mesurées aujourd'hui (jour 3 = audit positions, prochain mercredi).

## Blocages à traiter par le propriétaire (récurrents)

1. **Bing Webmaster Tools jamais claim** → IndexNow/Bing inactif depuis au moins le
   19/08. Action requise côté humain (compte Microsoft).
2. **Vercel MCP 403** sur `list_deployments`/`get_deployment` — permissions du
   connecteur à revoir, contourné par vérification de contenu live à chaque run.
3. **`seoBody` (contenu réel prerendu) non étendu** au nouvel article ni à la
   majorité du blog — arbitrage déjà demandé le 19/08, toujours en attente.

## Prochaine action

Samedi (jour 6) = optimisation technique Core Web Vitals (Lighthouse sur 3 pages).
