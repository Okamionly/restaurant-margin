# SEO Daily Push — Lundi 24/08/2026

## Décision du jour — écart assumé vs la rotation

La rotation par défaut du lundi est "nouvel article PAA". J'ai dévié
volontairement : la section PRIORITÉ ACTUELLE du SKILL (et la mémoire
`lessons_restaumargin_prerender_meta_only`, validée par A/B test le
23/08) établit que le vrai goulot n'est pas le volume d'articles mais
le fait que `prerender.cjs` ne sert qu'un **gabarit générique** (pas le
vrai contenu React) sur ~49 des ~51 articles blog — donc invisible pour
Google sur les requêtes informationnelles. Deux pages déjà corrigées
(`coefficient-multiplicateur`, `reduire-food-cost`) ont mesurablement
gagné en ranking après ce traitement. J'ai appliqué le même correctif
au candidat le plus prioritaire de tous : **le hub `/blog/calcul-marge-restaurant`**
(déjà #5 sur "marge restaurant", la requête cible n°1 du site) qui
servait encore le gabarit générique — c'était explicitement le
"candidat suivant probable" noté dans la mémoire du 23/08.

## Action réalisée

**Boost contenu réel (équivalent mardi, appliqué au hub le plus stratégique)**
- Extrait le vrai contenu du composant `BlogCalcMarge.tsx` (formules, benchmarks,
  exemple chiffré, erreurs courantes — pas de contenu inventé, tout provient
  de la page elle-même) et injecté via un champ `seoBody` dans `prerender.cjs`,
  même mécanisme que les 2 pages déjà corrigées.
- 5 sections H2 : 4 formules essentielles, marge brute vs marge nette, tableau
  benchmarks 7 types de restaurant, exemple chiffré burger maison, 5 erreurs
  courantes.
- `lastmod` bumpé au 2026-08-24 dans `sitemap.xml` (contenu réellement modifié,
  conforme à la règle anti-lastmod-fantaisiste de `lessons_seo_soft404_maillage`).

## Files modified
- `client/scripts/prerender.cjs` (+seoBody route `/blog/calcul-marge-restaurant`)
- `client/public/sitemap.xml` (lastmod)

## Commits pushed
- `7b26941` — seo(boost): seoBody reel pour /blog/calcul-marge-restaurant (hub, deja #5)

## Vérifications effectuées
- Build local (`vite build`) : OK, "files generated".
- Prerender local : `client/dist/blog/calcul-marge-restaurant/index.html` passe de
  gabarit générique à 25,6 Ko avec les 3 marqueurs de contenu réel présents.
- **Prod vérifiée en direct** (curl User-Agent Googlebot, poll jusqu'à propagation) :
  `HTTP 200`, title correct, contenu réel confirmé présent (25 661 octets, vs
  ~18 964 avant déploiement). `sitemap.xml` prod sert bien `lastmod` 2026-08-24
  pour cette URL.

## Vercel deploy status
⚠️ **Non vérifiable via le connecteur MCP Vercel** : `list_deployments` et
`list_projects` renvoient tous deux un résultat vide/403 pour le projet
`prj_SSuTV8ZC1E92w6RFGJudDUcX4Ay7` sur le team `team_eCxO0038Mvjt2u3s4zvhYzTj`
(le `list_teams` confirme pourtant le bon compte). Le connecteur semble avoir
perdu l'autorisation sur ce projet précis — à réautoriser côté user (`claude mcp`
ou paramètres du connecteur Vercel). **Contournement utilisé** : vérification
directe en prod par curl/poll (voir ci-dessus) — le déploiement a bien eu lieu
et le contenu est confirmé live, donc pas de blocage réel sur le fond, seulement
sur l'outil d'inspection.

## IndexNow (Bing/Yandex)
⚠️ **Nouveau blocage détecté** — `npm run indexnow` :
- `api.indexnow.org` → **403 `UserForbiddedToAccessSite`**
- `www.bing.com` → **403 `UserForbiddedToAccessSite`**
- `yandex.com` → 200 OK

Le fichier clé `2705a97b02bcbcaf1eb932313db3b7f8aea4901c38fe6379dbe56cce8184a951.txt`
est bien servi correctement en prod (vérifié par WebFetch, contenu exact).
Ce n'est donc pas un problème de fichier clé côté site, mais une désactivation
côté compte Bing (la vérification IndexNow/Bing Webmaster Tools a peut-être
expiré ou été révoquée). **Action user recommandée** : re-vérifier le site dans
Bing Webmaster Tools. Sans ça, le canal Bing (prioritaire d'après une inscription
antérieure) est mort jusqu'à nouvel ordre — seul Yandex reçoit encore les pings.

## Position Google
Non re-vérifiée aujourd'hui (dernière mesure connue : `coefficient multiplicateur
restaurant` → #1 le 23/08, mesure post-fix). Prochain point de mesure logique :
"marge restaurant" pour voir si le hub progresse depuis #5 suite à ce fix.

## Next action (mardi 25/08)
Rotation mardi = "boost article existant". Continuer le chantier `seoBody` sur
le prochain candidat prioritaire — recommandation : `/blog/faq-marge-restaurant-25-questions`
ou `/blog/marge-beneficiaire-restaurant-ideal` (autres pages à forte intention
informationnelle sur "marge restaurant", probablement encore au gabarit — à
vérifier via `grep -c "seoBody" client/scripts/prerender.cjs`, actuellement 3/51
articles traités). Vérifier aussi le statut GSC de `/alternative-innovorder`
(toujours non-crawlée au dernier check du 23/08) et re-tenter IndexNow demain
pour voir si le blocage Bing persiste (transitoire vs compte à ré-autoriser).
