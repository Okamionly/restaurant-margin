# SEO Daily Push — Mardi 25/08/2026

## Rotation du jour
Mardi = "boost article existant faible rank". J'ai suivi la recommandation
explicite laissée dans `reports/seo-daily-2026-08-24.md` ("Next action mardi
25/08 : continuer le chantier seoBody... prochain candidat prioritaire") et
choisi `/blog/menu-engineering-boston-matrix` : un des mots-clés cités
explicitement comme non-rankants dans la section PRIORITÉ ACTUELLE du skill,
page existante mais stale (dateModified inchangé depuis 6 semaines) et
figurant parmi les ~48/51 articles encore non couverts par le correctif
`seoBody` (cf. `lessons_restaumargin_prerender_meta_only`).

## Actions réalisées (2 commits complémentaires)

**1. Boost contenu réel dans le composant React**
- Nouvelle section 10 "Excel, export caisse ou logiciel dédié : quand
  automatiser" (~600 mots) : comparatif 3 niveaux d'outillage (tableur
  manuel / export caisse + tableur / logiciel dédié), 3 signaux de
  déclenchement, 4 nouveaux liens internes (`logiciel-caisse-enregistreuse-restaurant`,
  `logiciel-gestion-restaurant`, `outils/calculateur-food-cost`).
- +2 questions FAQ (schema `buildFAQSchema` **et** affichage JSX, tenus
  synchronisés — les deux tableaux existaient déjà séparément dans ce fichier).
- `dateModified` (schema Article + `BlogAuthor`) et `sitemap.xml` `lastmod`
  rafraîchis : 2026-07-10 → 2026-08-25.

**2. Correctif `seoBody` (continuité du chantier ouvert le 23-24/08)**
- Sans `seoBody`, cette page ne servait aux crawlers sans JS que le gabarit
  générique de catégorie "article" (comme ~48 autres pages blog) — le boost
  ci-dessus aurait donc été invisible pour Googlebot au premier passage HTML.
- Contenu réel (non inventé, extrait du composant) injecté dans
  `client/scripts/prerender.cjs` : définition, tableau des 4 quadrants,
  méthode de calcul, exemple chiffré "Le Comptoir", + la nouvelle section
  automatisation. **4ᵉ page traitée sur 51** (après `calcul-marge-restaurant`,
  `reduire-food-cost`, `coefficient-multiplicateur`).

## Files modified
- `client/src/pages/BlogMenuEngineering.tsx`
- `client/public/sitemap.xml`
- `client/scripts/prerender.cjs`

## Commits pushed
- `33a90d1` — seo(boost): renforce menu-engineering-boston-matrix +section auto + 2 FAQ + refresh
- `a1be8fa` — seo(boost): seoBody reel pour /blog/menu-engineering-boston-matrix

## Vérifications effectuées
- Build local (`vite build`) : OK, "files generated". `tsc --noEmit` : 0 erreur.
- Prerender local : `dist/blog/menu-engineering-boston-matrix/index.html` passe
  de gabarit générique à **23 837 octets** avec les marqueurs de contenu réel
  présents (comparé aux ~19 Ko habituels du gabarit générique).
- **Prod vérifiée en direct** (poll curl User-Agent Googlebot jusqu'à
  propagation, 2 déploiements) : `HTTP 200`, contenu réel confirmé présent
  (23 560 octets). `sitemap.xml` prod sert `lastmod` 2026-08-25 pour cette URL.
- Vérification visuelle navigateur (rendu React côté client) : section 10
  présente dans le DOM, TOC à jour, "Mis à jour le 25 août 2026" affiché,
  aucune casse de mise en page.

## Vercel deploy status
⚠️ Non vérifiable via le connecteur MCP Vercel : `list_deployments` renvoie
`403 Forbidden` ("You don't have permission to list the deployment") et
`get_deployment` par hostname renvoie `404 Deployment not found`, pour le
projet `prj_SSuTV8ZC1E92w6RFGJudDUcX4Ay7` / team `team_eCxO0038Mvjt2u3s4zvhYzTj`
— même symptôme que le 24/08 (connecteur toujours désautorisé sur ce projet
précis). **Contournement identique** : vérification directe en prod par
curl/poll (voir ci-dessus, 2 déploiements confirmés live) — pas de blocage
réel sur le fond, seulement sur l'outil d'inspection Vercel.

## IndexNow (Bing/Yandex)
Même blocage que le 24/08, toujours actif : `api.indexnow.org` et
`www.bing.com` → 403 `UserForbiddedToAccessSite` ; `yandex.com` → 200 OK
(2 pings envoyés aujourd'hui, un par déploiement). Le fichier clé est
toujours servi correctement en prod (vérifié). Confirme que ce n'est pas
transitoire : le canal Bing reste mort depuis au moins 48h. **Action user
requise** : re-vérifier le site dans Bing Webmaster Tools pour rétablir
IndexNow/Bing — ce n'est pas quelque chose qu'un agent peut résoudre depuis
le repo.

## Position Google
Non re-vérifiée aujourd'hui (dernière mesure connue : "coefficient
multiplicateur restaurant" → #1 le 23/08). Prochain point de mesure logique :
"menu engineering restaurant" pour observer l'effet de ce double correctif
(contenu + crawlabilité) sur 1-2 semaines.

## Next action (mercredi 26/08)
Rotation mercredi = "audit positions + analyse concurrent". Suggestions :
1. Mesurer les 5 requêtes clés du skill (marge restaurant, food cost
   restaurant, coefficient multiplicateur restaurant, calcul marge
   restaurant, logiciel marge restaurant) pour objectiver l'effet cumulé des
   corrections `seoBody` des 4 derniers jours.
2. Continuer le chantier `seoBody` : candidats suivants les plus stratégiques
   — `/blog/prime-cost-restaurant`, `/blog/prix-de-vente-restaurant` ou
   `/blog/seuil-rentabilite-restaurant` (mêmes mots-clés prioritaires cités
   dans le skill, encore non traités — vérifier via
   `grep -c "seoBody:" client/scripts/prerender.cjs`, actuellement 4/51).
3. Relancer IndexNow pour confirmer si le blocage Bing persiste toujours
   (3 jours consécutifs maintenant) — si oui, ce point mérite d'être remonté
   explicitement à l'utilisateur plutôt que redécouvert chaque jour.
