# SEO Daily Push — 2026-08-31 (Lundi)

## Décision du jour : dérogation à la rotation

La rotation prévoit "Lundi = nouvel article PAA". J'ai dérogé, en application explicite de la
section **PRIORITÉ ACTUELLE** en tête du run (« le levier n'est PAS de produire toujours plus
d'articles ») : `docs/blog-articles/backlog.md` listait 5 articles rédigés en `.md` mais jamais
componentisés/routés depuis 6 semaines (10/07 → 21/08). Écrire un 6e article pendant que 5
dormaient allait dans le sens inverse de la priorité affichée.

**Audit avant action** : sur les 5, seuls 3 étaient réellement non publiés. `ouvrir-terrasse-restaurant-demarches`
et `reprise-restaurant-guide-acheteur` avaient en fait déjà leur route dans `App.tsx` + leur entrée
dans `sitemap.xml` depuis leur rédaction — le backlog.md ne l'avait simplement jamais reflété (état
stale, jamais vérifié par les runs précédents faute de `grep` croisé). Corrigé au passage.

## Action réalisée

Publication groupée des 3 articles réellement en attente :

| Slug | Mots | Catégorie |
|---|---|---|
| `tva-restauration-taux-guide` | 2 603 | Réglementation |
| `licence-iv-restaurant-guide` | 3 049 | Réglementation |
| `avis-google-negatifs-restaurant` | 2 508 | Marketing |

Pour chacun : composant `.tsx` (pattern W&B / `BlogArticleHero` + `BlogAuthor`, identique à
`BlogRepriseRestaurant.tsx`), route lazy dans `App.tsx`, entrée `prerender.cjs` (title+description
identiques au `SEOHead` React), entrée `sitemap.xml` (priority 0.7, monthly), carte dans
`BlogIndex.tsx`, Schema Article + FAQPage + BreadcrumbList, maillage interne croisé entre les 3 +
vers 4 articles publiés existants (aucun lien mort — vérifié par grep des routes cibles avant écriture).

## Files modified

- `client/src/pages/BlogTvaRestaurationTaux.tsx` (nouveau)
- `client/src/pages/BlogLicenceIV.tsx` (nouveau)
- `client/src/pages/BlogAvisGoogleNegatifs.tsx` (nouveau)
- `client/src/App.tsx` (3 imports + 3 routes)
- `client/src/pages/BlogIndex.tsx` (3 cartes)
- `client/scripts/prerender.cjs` (3 entrées)
- `client/public/sitemap.xml` (3 URLs)
- `docs/blog-articles/backlog.md` (statuts corrigés + note du gap trouvé)

## Vérifications effectuées

- `npm run seo:check` → OK, sitemap (124 URLs) et prerender (123 routes) synchronisés
- `npx vite build` → succès (`files generated`, PWA precache OK)
- `npx tsc -b --noEmit` → 0 erreur
- grep ` > `/` < ` non échappés → aucun
- Les 3 pages vérifiées **en prod** via Googlebot UA après déploiement : HTTP 200, title prérendu
  correct sur les 3 (confirmé, pas supposé)
- `sitemap.xml` prod contient les 3 nouvelles URLs (124 `<loc>` au total)

## Commit pushed

`220f608` — `seo(publication-lot): publie 3 articles rediges en attente depuis 6 semaines`
→ `origin/main` (ac08628..220f608)

## Vercel

Déploiement confirmé **indirectement** : les 3 pages servent le HTML prérendu correct en prod
~15 s après le push (vérifié par poll Googlebot UA). Le MCP Vercel (`list_deployments`) a renvoyé
**403 Forbidden** ("You don't have permission to list the deployment") sur ce projet/team — à
vérifier côté user (permissions token). Pas bloquant aujourd'hui, la preuve directe (contenu servi)
est plus fiable qu'un statut d'API de toute façon.

## IndexNow

- Yandex : **HTTP 200 OK**
- Bing (`api.indexnow.org` + `www.bing.com`) : **HTTP 403 `UserForbiddedToAccessSite`**, alors que
  le fichier clé (`.../2705a97b...951.txt`) est vérifié servi correctement (200, text/plain, contenu
  exact). Nouveau — les runs précédents (dont le plus récent noté en mémoire) obtenaient 202 Accepted
  sur Bing avec la même clé. Pas de cause identifiée aujourd'hui (hors scope d'investigation
  immédiate — nécessiterait Bing Webmaster Tools). **À surveiller / creuser au prochain run.**

## Gap découvert (pour un prochain run)

`BlogIndex.tsx` ne liste que 45 des 59 pages `.tsx` de blog publiées — au moins 11 articles
(dont plusieurs assez récents) sont orphelins de la page `/blog` : accessibles par URL directe et
présents dans le sitemap, mais sans lien découvrable depuis l'index. Bon candidat pour l'action
mardi/mercredi (boost + maillage interne).

## Next action (mardi)

Rotation mardi = "Boost article existant faible rank". Candidat suggéré : combler le gap de
maillage `BlogIndex.tsx` ci-dessus (11 articles orphelins), ou reprendre un article
sous-dimensionné listé dans la note de comptage du backlog (`terrasse`, `kpi-essentiels`,
`livraison`, `food-truck`, `contrat-travail`, `énergie`, `fifo-lifo` — tous < 1 500 mots réels).
