# SEO Daily Push — Jeudi 2026-09-03

## Action réalisée : nouvelle page comparatif (rotation jeudi)

Plutôt que de piocher dans la liste générique de la routine (Tiller, Sunday, Marketman, Easilys,
Deliverect, Skello, Inpulse), j'ai ciblé un concurrent **réel et déjà identifié en mémoire projet** :
**COS Kitchen** (coskitchen.fr), repéré le 2026-08-19 comme #1 sur une requête commerciale
(`logiciel marge restaurant food cost IA`) et jamais couvert par une page comparatif. Contrairement
aux pages `alternative-*` existantes (Zenchef, Innovorder, Hubrise, Lightspeed, L'Addition, TheFork —
toutes des logiciels de caisse/réservation), COS Kitchen est un **concurrent direct** sur le calcul de
food cost, donc une cible plus pertinente que la liste générique du SKILL.md.

Contenu vérifié via WebSearch + WebFetch sur coskitchen.fr (accueil + `/tarifs`) le jour même — aucune
donnée inventée sur le concurrent :
- Plan Découverte gratuit à vie (10 fiches techniques, 1 menu)
- Plan Essentiel 29€ HT/mois (fiches/menus illimités, IA d'optimisation)
- Plan Pro 59€ HT/mois (suivi fournisseurs, alertes prix — équivalent RestauMargin inclus dès 29€)
- 2 500+ restaurateurs revendiqués, "15% économies moyennes"

## Files modified

- `client/src/pages/AlternativeCosKitchen.tsx` (nouveau, ~2 700 mots, pattern `AlternativeZenchef.tsx`)
- `client/src/App.tsx` (import lazy + route `/alternative-cos-kitchen`)
- `client/public/sitemap.xml` (+1 URL, priority 0.85, lastmod 2026-09-03)
- `client/scripts/prerender.cjs` (+1 entrée title/description)

Schema markup : FAQPage (10 Q/R) + BreadcrumbList + Article + ItemList (SoftwareApplication x2).

## Commits pushed

- `573b80a` — seo(comparatif): nouvelle page alternative COS Kitchen — concurrent food cost identifie

## Vérifications

- `npx tsc --noEmit` : 0 erreur
- `npx vite build` : OK ("files generated")
- `node scripts/prerender.cjs` : 124 fichiers générés (+1)
- `node scripts/check-seo-coverage.cjs` : OK — sitemap 125 URLs / prerender 124 routes, toutes couvertes
- Prod vérifiée en direct (Googlebot UA + navigateur) : title correct servi, contenu FAQ/tableau rendu,
  0 erreur console liée à la page (une seule erreur CSP pré-existante, site-wide, sans rapport — tâche
  séparée proposée : `task_a3dade5b`)

## Vercel deploy status

**Non vérifiable via MCP aujourd'hui** — `list_deployments` a renvoyé `403 Forbidden`, et
`list_projects` sur `team_eCxO0038Mvjt2u3s4zvhYzTj` retourne une liste **vide** alors que `list_teams`
fonctionne normalement (le projet `prj_SSuTV8ZC1E92w6RFGJudDUcX4Ay7` existe pourtant : `get_project`
renvoie 404 sur cet ID). Anomalie de portée/permissions du connecteur Vercel à vérifier côté user —
possiblement un changement de scope OAuth ou un token expiré. **Contournement appliqué** : vérification
directe en production par polling `curl` (Googlebot UA) toutes les 20s — le nouveau title est apparu
en moins de 20s après le push, confirmant un déploiement réussi malgré l'indisponibilité du MCP.

## IndexNow

`npm run indexnow` : 125 URLs soumises. Bing/`api.indexnow.org` toujours en **403
`UserForbiddedToAccessSite`** (régression connue depuis le 2026-08-31, 4ᵉ jour consécutif — nécessiterait
Bing Webmaster Tools pour diagnostiquer, hors scope de cette routine). Yandex : 200 OK.

## Position Google

Non re-vérifiée aujourd'hui (audit dédié = mercredi). Dernière mesure (2026-09-02) : clivage stable,
RestauMargin domine le commercial, absent de l'informationnel pur. Voir mémoire
`project_restaumargin_seo_positions.md`.

## Next action (vendredi — sitemap fresh + rich snippets)

- Update lastmod sur les 10 pages les plus importantes
- Vérifier prod sert bien la version fraîche du sitemap
- Test Googlebot UA sur 5 pages clés
- **Prioritaire à investiguer** : la panne du MCP Vercel (list_deployments 403 + list_projects vide) —
  si elle persiste vendredi, le contournement curl/Googlebot devra devenir la méthode par défaut de
  vérification de déploiement pour cette routine.
