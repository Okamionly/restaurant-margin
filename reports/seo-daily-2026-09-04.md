# SEO Daily Push — Vendredi 2026-09-04

## Action réalisée : sitemap fresh + ping IndexNow + audit rich snippets

Rotation du vendredi : rafraîchissement `lastmod` + vérification propagation prod + contrôle
Googlebot + audit schema markup.

### 1. Sitemap freshness

`lastmod` mis à jour à `2026-09-04` sur les 10 pages jugées les plus importantes (home, pricing,
launch, hub `/logiciel-marge-restaurant`, hub blog `/blog/calcul-marge-restaurant`,
`/outils/calculateur-marge-restaurant`, `/comparatif-logiciels-restaurant`,
`/comparatif-restaumargin-vs-zenchef`, `/glossaire-restauration`,
`/blog/faq-marge-restaurant-25-questions`).

### 2. Vérification prod

Poll direct sur `https://www.restaumargin.fr/sitemap.xml` après push : la homepage servait encore
`2026-08-21` ~1min30 après le push (déploiement Vercel en cours), confirmée fraîche (`2026-09-04`)
au poll suivant (~2min30 post-push). Propagation normale.

### 3. Test Googlebot UA (5 pages clés)

Title + meta description vérifiés via `curl -A Googlebot` sur `/`, `/blog/calcul-marge-restaurant`,
`/logiciel-marge-restaurant`, `/comparatif-logiciels-restaurant`, `/glossaire-restauration` — les 5
servent le title/description attendu (prerender fonctionne correctement, aucun fix nécessaire côté
`prerender.cjs`).

### 4. Audit rich snippets

Structured data vérifié en direct (Googlebot UA) :
- Homepage : `Organization`, `WebSite`, `SoftwareApplication`, `AggregateOffer` — **pas
  d'`aggregateRating`** (le faux avis 4.8/150 signalé dans l'audit du 2026-07-07 reste absent,
  confirmé de nouveau dans `client/index.html`).
- `/blog/calcul-marge-restaurant` et `/comparatif-logiciels-restaurant` : `BreadcrumbList` présent.

Aucune anomalie de rich snippets détectée aujourd'hui.

## Files modified

- `client/public/sitemap.xml` (10 `lastmod` rafraîchis)

## Commits pushed

- `bd6af33` — seo(freshness): sitemap update + audit rich snippets

## Vérifications

- `npx vite build` (client) : OK ("files generated")
- Prod re-pollée post-push : sitemap frais confirmé sur `/`

## Vercel deploy status

**Toujours non vérifiable via MCP** — `list_deployments` → `403 Forbidden`, `list_projects` sur
`team_eCxO0038Mvjt2u3s4zvhYzTj` → liste vide (alors que `list_teams` fonctionne). 5ᵉ jour consécutif
de cette anomalie (régression depuis le 2026-08-31). **Contournement** : polling `curl` direct sur le
sitemap prod (comme les jours précédents) — a confirmé le déploiement réussi. Recommandation
inchangée : vérifier côté user si le token/scope OAuth du connecteur Vercel a expiré ou changé.

## IndexNow

`npm run indexnow` : 126 URLs soumises. `api.indexnow.org` et `www.bing.com` → toujours **403
`UserForbiddedToAccessSite`** (régression connue depuis le 2026-08-31, 5ᵉ jour consécutif) —
vérifié que ce n'est PAS un problème de fichier clé : `https://www.restaumargin.fr/2705a97b...txt`
répond `200 OK` avec le bon contenu (pas de redirection, host exact). Le blocage est donc côté
vérification Bing elle-même, pas côté déploiement — nécessiterait une ré-authentification via Bing
Webmaster Tools (hors scope de cette routine, action manuelle user). Yandex : `202 Accepted`,
propagation réseau IndexNow effective.

## Position Google

Non re-vérifiée aujourd'hui (audit dédié = mercredi, prochain point le 2026-09-09). Dernière mesure
(2026-09-02) : clivage stable, RestauMargin domine le commercial, absent de l'informationnel pur.

## Next action (samedi — optimisation technique)

- Lighthouse / Core Web Vitals sur homepage + `/blog/calcul-marge-restaurant` + `/pricing`
- Identifier 1 optimisation (lazy loading images, preload assets, bundle size)
- **Point de suivi à 6 jours** : si le 403 Bing IndexNow persiste lundi, envisager de signaler
  explicitement au user que Bing Webmaster Tools nécessite une action manuelle (ré-vérification du
  site) — la routine ne peut pas la faire seule.
