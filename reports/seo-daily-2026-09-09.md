# SEO Daily Push — 2026-09-09 (Mercredi)

## Action réalisée : Audit positions Google + analyse concurrent + amélioration appliquée

Détail complet dans [`reports/seo-audit-2026-09-09.md`](./seo-audit-2026-09-09.md).

### Résumé
- **5 requêtes vérifiées** via scrape SERP réel (Playwright, `google.com/search?gl=fr&hl=fr`, pas WebSearch — cf. note méthodologique dans le rapport d'audit). RestauMargin **#1** sur `logiciel marge restaurant`, **absent du top 10** sur les 4 requêtes informationnelles (`marge restaurant`, `food cost restaurant`, `coefficient multiplicateur restaurant`, `calcul marge restaurant`). Clivage commercial/informationnel confirmé pour la Nᵉ fois.
- **Concurrent analysé** : RestoPilot (#2 sur `coefficient multiplicateur restaurant`). Diagnostic : pas un problème de volume de contenu (page RestauMargin plus riche), mais une FAQ de 19 questions jamais dupliquée dans le `seoBody` prerendu — candidat déjà identifié le 02/09, resté en attente jusqu'à ce run.
- **Amélioration appliquée** : 10 Q/R dupliquées verbatim (texte identique au composant React, pas d'invention) dans `client/scripts/prerender.cjs` pour `/blog/coefficient-multiplicateur`, sous un nouveau H2 + 10 H3. Vérifié dans le HTML généré localement (`dist/blog/coefficient-multiplicateur/index.html`).
- **4 nouveaux concurrents informationnels repérés** (earn.fr, gostan.io, asendens.fr, sumup.com) — à requalifier sur 2-3 runs avant d'agir.

## Files modified
- `client/scripts/prerender.cjs`
- `reports/seo-audit-2026-09-09.md` (nouveau)

## Commits pushed
- `6ee5090` — seo(audit): rapport positions + FAQ seoBody coefficient-multiplicateur

## Vercel deploy status
**⚠️ NON CONFIRMÉ — à vérifier au prochain run.** `mcp__vercel__list_deployments` renvoie **403 Forbidden** (blocage déjà documenté, non résolu, identique aux runs précédents). Tentative de contournement via `get_deployment_build_logs` sur le domaine → 404 (pas d'ID de déploiement exploitable sans `list_deployments`).

Vérification live par `curl -A Googlebot` sur `/blog/coefficient-multiplicateur`, répétée 4 fois sur ~10 minutes après le push : **HTTP 200 mais le nouveau contenu FAQ (H2/H3 ajoutés) n'apparaît toujours pas** dans le HTML statique servi. Sanity check de contrôle sur `/blog/calcul-marge-restaurant` (fix FAQ du 02/09, stable) : **confirmé toujours présent** (8 `<h3>`) — donc le mécanisme de prerender/déploiement fonctionne globalement, ce n'est pas un problème d'infrastructure générale. Reste deux hypothèses non tranchées faute d'accès au statut du build : (1) déploiement encore en cours au-delà de la fenêtre observée ce run, (2) build en échec pour une autre raison. Build local (`npx vite build` + `node scripts/prerender.cjs`) confirmé réussi avant push — donc si le déploiement Vercel échoue, ce n'est pas une erreur de code détectable en local.

**Action pour le prochain run** : revérifier `/blog/coefficient-multiplicateur` (via `curl -A Googlebot`, chercher `<h3>` et le texte "Questions frequentes sur le coefficient multiplicateur") avant toute nouvelle action de rotation. Si toujours absent après 24h, creuser le webhook GitHub→Vercel (cf. [[lessons]] : "si aucun build Vercel ne part après push, webhook possiblement en rade → API POST /v13/deployments direct").

## Position Google
Voir tableau complet dans le rapport d'audit — 5 requêtes vérifiées ce run (c'est la tâche du jour).

## Next action (jeudi — jour 4)
Nouvelle page niche ou comparatif. Candidats en attente : Marge Brut (position à reconfirmer) ou l'un des 4 nouveaux concurrents informationnels repérés aujourd'hui — trancher après 1-2 runs de données supplémentaires. **Vérifier en premier le statut du déploiement d'aujourd'hui** avant d'empiler une nouvelle action.
