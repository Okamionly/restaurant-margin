# SEO Daily Push — 2026-09-10 (Jeudi)

## Action réalisée : nouvelle page comparatif — Alternative Marge Brut

Concurrent choisi : **Marge Brut** (margebrut.fr), identifié comme concurrent réel dans l'audit du
02/09 (position #2 sur `logiciel marge restaurant food cost IA`), noté "à couvrir" dans la mémoire
projet pour un futur jeudi. Données vérifiées via WebSearch + WebFetch sur margebrut.fr le jour même
(pas d'invention) : logiciel gratuit en phase de développement actif (food cost temps réel, lecture IA
factures, P&L quotidien, trésorerie 13 semaines, gestion équipe, cave à vin), sans date de bascule vers
un modèle payant communiquée.

Page créée sur le pattern `AlternativeCosKitchen.tsx` (Article + FAQPage + BreadcrumbList + ItemList
schema, ~2 700 mots, 10 FAQ, tableau comparatif 15 critères, cas d'usage, comparaison prix 12 mois).
**Écart volontaire au template** : la section "Témoignages" (avis clients fictifs à prénom modifié) du
template de référence n'a pas été reprise — remplacée par une section factuelle sur la pérennité du
modèle économique gratuit de Marge Brut, conformément à la règle prioritaire du SKILL ("jamais de faux
avis") et à la doctrine mémoire `no_fake_eat_signals`.

**Vérification préalable anti-doublon** : `git log` + `docs/actions/*-2026-09-10.md` +
`docs/reports/*-2026-09-10.md` du système multi-agent "workforce" (onboarder/cmo/ceo, tourne en
parallèle sur ce repo) consultés avant de choisir la tâche — aucun commit SEO/blog de leur part
aujourd'hui, pas de doublon.

## Files modified
- `client/src/pages/AlternativeMargeBrut.tsx` (nouveau)
- `client/src/App.tsx` (route + lazy import)
- `client/public/sitemap.xml` (+1 URL, priority 0.85)
- `client/scripts/prerender.cjs` (title + description)

## Vérifications avant push
- `npx tsc --noEmit` (client) — OK, 0 erreur
- `node client/scripts/check-seo-coverage.cjs` — OK, 129 sitemap / 128 prerender, toutes les URLs couvertes
- `npx vite build` — OK, "files generated"
- `node client/scripts/prerender.cjs` — 128 fichiers générés, `<title>` confirmé dans le HTML local
- `grep -n " > "` sur le nouveau fichier — aucun caractère `>` problématique

## Commits pushed
- `894e0c2` — seo(niche|comparatif): nouvelle page Alternative Marge Brut

## Vercel deploy status
**✅ READY, confirmé sur les deux projets liés au repo.** `mcp__vercel__list_deployments` toujours en
403 Forbidden (blocage connu, non résolu côté MCP) — contournement `gh api
repos/Okamionly/restaurant-margin/commits/894e0c2/status` utilisé directement (pas de curl répétés).
Séquence observée : `pending` (Vercel – restaumargin) / `success` (Vercel – restaurant-margin) juste
après le push, puis **`success` sur les deux contextes** ~4 min plus tard. `curl -A Googlebot` sur
`/alternative-marge-brut` confirme le `<title>` correct en prod, `X-Vercel-Cache: MISS` (pas de retard
de cache cette fois, contrairement au 09/09).

## IndexNow (Bing/Yandex)
`npm run indexnow` exécuté après le push (129 URLs). **Bing toujours 403
`UserForbiddedToAccessSite`** (persistant depuis le 31/08, état confirmé identique une fois de plus —
ne plus signaler comme anomalie nouvelle). **Yandex OK (200)**, seul canal IndexNow fonctionnel
actuellement.

## Position Google
Non vérifiée ce run (tâche du mercredi, dernière mesure le 09/09 — voir `reports/seo-audit-2026-09-09.md`).

## Next action (vendredi — jour 5)
Sitemap fresh + ping + audit rich snippets sur 5 pages clés. Ajouter `/alternative-marge-brut` à la
liste des pages à vérifier en Googlebot UA (nouvelle page du jour). Vérifier aussi si `BlogIndex.tsx`
ou une page "Comparatifs" doit lister les alternatives (aucune des 8 pages `Alternative*.tsx`
existantes, y compris celle-ci, n'apparaît dans `BlogIndex.tsx` — gap déjà connu, non traité
aujourd'hui, hors scope de la rotation jeudi).
