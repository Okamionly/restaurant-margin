# SEO Daily Push — dimanche 2026-09-13

## Rotation du jour : review hebdo + plan semaine + EAT

**Action EAT refusée** (comme chaque dimanche depuis le 23/08) : le brief demande un "author bio avec lien LinkedIn fictif" ou une "mention presse fictive" — signaux de confiance fabriqués, même famille de risque que le faux `aggregateRating` déjà retiré (action manuelle Google, exposition du propriétaire). Non exécuté. Substitué par un fix bloquant + la publication d'une page orpheline (voir ci-dessous et le rapport hebdo).

## STEP 1 — Vérifications de départ

- `git fetch` + `git pull` : **divergence** — 1 commit local (rattrapage blog-writer du 12/09) vs 8 commits distants du système "workforce" (cto/coo/cmo/qa/cfo/ceo/onboarder/outreach + blog-writer). Résolu par `git merge` sans conflit (fichiers disjoints).
- Vercel : dernier déploiement AVANT ce run était `success` sur les 2 contextes (`restaumargin` + `restaurant-margin`).
- **Blocker détecté** : le `qa` du workforce (`38c5e11`, 12/09 18h28) avait déjà loggé un build TypeScript cassé (`BlogLogicielReservation.tsx:92`, prop `canonicalPath` inexistante sur `SEOHeadProps`) mais aucun fix n'avait été poussé — traité en priorité avant toute autre action (règle STEP 1 : "Si ERROR, fix avant de continuer").

## Action principale

1. **Fix build** : `canonicalPath` → `path` (seule prop supportée par `SEOHeadProps`).
2. **Publication de la page orpheline `logiciel-reservation-restaurant`** : componentisée + sitemap par le workforce le 12/09, mais jamais routée/prerendue/indexée dans `BlogIndex.tsx`. Ajouté :
   - Route `App.tsx` (lazy import + `<Route>`)
   - Entrée `client/scripts/prerender.cjs` (title + description repris du composant, aucune invention)
   - Carte `client/src/pages/BlogIndex.tsx` (catégorie "Outils", cohérente avec les catégories existantes)
3. Vérifications : `npx vite build` OK, `node scripts/prerender.cjs` → 129 fichiers, `node scripts/check-seo-coverage.cjs` → sitemap/prerender en parité, title prerendu vérifié dans `dist/`.
4. Weekly review complet : `reports/seo-weekly-2026-37.md` (positions, commits de la semaine, plan semaine 38).

## Files modified

- `client/src/pages/BlogLogicielReservation.tsx` (fix prop)
- `client/src/App.tsx` (route)
- `client/scripts/prerender.cjs` (entrée SEO)
- `client/src/pages/BlogIndex.tsx` (carte index)
- `reports/seo-weekly-2026-37.md` (nouveau)

## Commits pushed

- `29a4aa0` — fix(seo): régression build TS + page orpheline logiciel-reservation-restaurant

## Vercel deploy status

**success** sur les 2 contextes (`Vercel – restaumargin`, `Vercel – restaurant-margin`), confirmé via `gh api repos/Okamionly/restaurant-margin/commits/29a4aa0.../status` après ~2 min. Title prerendu vérifié en prod via curl Googlebot UA.

## Position Google (comparaison semaine, détail dans le rapport hebdo)

- `logiciel marge restaurant` : **#1** (stable)
- `marge restaurant calcul logiciel` : **#1** (progression, était #5)
- `coefficient multiplicateur restaurant calcul` : **absent top 8** (régression, était #4 stable — à remesurer mercredi)
- `prime cost restaurant calcul formule` : **absent top 9** (inchangé, 2 semaines après fix `seoBody`)

## IndexNow

`npm run indexnow` exécuté après les changements de routing (130 URLs) : Bing/api.indexnow.org toujours 403 `UserForbiddedToAccessSite` (persistant depuis fin août, action propriétaire requise), Yandex 202 Accepted.

## Dette signalée pour la semaine prochaine

- `pourboires-restaurant-legislation-fiscalite.md` (rédigé 12/09 par blog-writer) : pas encore componentisé/publié.
- Recommandation répétée au propriétaire : brancher `check-seo-coverage.cjs` en fin de run `blog-writer` pour arrêter la récidive du gap de publication (4ᵉ occurrence en 3 semaines).

## Next action (lundi 14/09)

Rotation lundi = nouvel article PAA, sauf si `pourboires-restaurant-legislation-fiscalite` reste non publié — dans ce cas, priorité à la publication de l'existant (doctrine confirmée les 31/08 et 07/09).
