# SEO Daily Push — lundi 2026-09-14

## Rotation du jour : nouvel article PAA — DEVIÉE vers publication de l'existant

Doctrine confirmée les 31/08, 07/09 et 13/09 : avant tout nouvel article, vérifier que `pourboires-restaurant-legislation-fiscalite` (rédigé par `blog-writer` mais jamais componentisé) est enfin publié. **Toujours pas publié** — 4ᵉ jour consécutif où le gap était signalé sans action. Priorité donnée à la publication de cet article plutôt qu'à la rotation lundi (nouvel article PAA), conformément à la doctrine et à la priorité "pousser l'existant" en tête de ce brief.

## STEP 1 — Vérifications de départ

- `git fetch` + `git pull --ff-only` : OK, fast-forward propre (10 commits du système "workforce" récupérés : coo/cto/qa/cmo/ceo/onboarder/bug-fixer, sans conflit).
- **Vercel MCP `list_deployments` / `get_project` / `list_projects` : 403/404, le connecteur ne voit plus le projet** (team ID confirmé correct via `list_teams`, mais 0 projet retourné). Contournement : vérification directe `curl` (homepage 200, sitemap accessible) + `gh api .../commits/<sha>/status` après push, qui reste fiable.

## Action principale

**Publication de la page orpheline `pourboires-restaurant-legislation-fiscalite`** (source : `docs/blog-articles/pourboires-restaurant-legislation-fiscalite.md`, ~2600 mots, sourcé WebSearch septembre 2026 — exonération pourboires prorogée jusqu'au 31/12/2028, plafond 1,6 SMIC, répartition tronc collectif, cas chiffré 25 couverts/service) :

1. Componentisation en `client/src/pages/BlogPourboiresRestaurant.tsx` (template `BlogChargesSociales.tsx` : SEOHead + FAQ 5 Q/A + Article + Breadcrumb schema, `BlogAuthor`, `BlogArticleHero`).
2. Route ajoutée dans `App.tsx` (lazy import + `<Route>`).
3. Entrée `client/scripts/prerender.cjs` (title + description).
4. Entrée `client/public/sitemap.xml` (priority 0.85, lastmod 2026-09-14).
5. Carte `client/src/pages/BlogIndex.tsx` (catégorie "Juridique").
6. **Maillage interne** : lien entrant ajouté depuis `BlogChargesSociales.tsx` (section pièges, item "Pourboires mal traités") vers le nouvel article — au passage, correction d'une date d'exonération obsolète dans ce même fichier (31/12/2026 → 31/12/2028, alignée sur la LF 2026 confirmée par la nouvelle source).

## Vérifications

- `npx vite build` : OK.
- `node scripts/prerender.cjs` : 130 fichiers générés.
- `node scripts/check-seo-coverage.cjs` : sitemap (131 URLs) / prerender (130 routes) en parité — OK.
- Title prerendu vérifié dans `dist/` puis en prod via `curl` Googlebot UA : `Pourboires en restauration : legislation, fiscalite et repartition 2026 | RestauMargin` — conforme.
- Sitemap prod (`curl https://www.restaumargin.fr/sitemap.xml`) : nouvelle URL présente.

## Files modified

- `client/src/pages/BlogPourboiresRestaurant.tsx` (nouveau)
- `client/src/App.tsx` (route)
- `client/scripts/prerender.cjs` (entrée SEO)
- `client/public/sitemap.xml` (nouvelle URL)
- `client/src/pages/BlogIndex.tsx` (carte index)
- `client/src/pages/BlogChargesSociales.tsx` (lien entrant + fix date exonération)

## Commits pushed

- `bd301b3` — seo(paa-daily): publie l'article orphelin pourboires-restaurant-legislation-fiscalite

## Vercel deploy status

**success** sur les 2 contextes (`Vercel – restaumargin`, `Vercel – restaurant-margin`), confirmé via `gh api repos/Okamionly/restaurant-margin/commits/bd301b3.../status` (~2 min 30 après push). Vérification directe car le MCP Vercel ne retourne plus le projet (voir STEP 1 — à signaler au propriétaire, reconnexion du connecteur probablement nécessaire).

## Position Google (mesure WebSearch — voir note fiabilité)

- `coefficient multiplicateur restaurant calcul` : **#1** (page `/blog/coefficient-multiplicateur`) — retour après l'absence notée hier, confirme la volatilité de cette méthode de mesure plutôt qu'une vraie régression.
- `marge restaurant calcul logiciel` : **#4** (page `/blog/fiche-technique-restaurant`, pas la hub `/blog/calcul-marge-restaurant`) — hier annoncé #1 sur le même libellé. Écart jour à jour important.
- **Note de fiabilité** : l'outil `WebSearch` utilisé pour ce tracking n'est pas le vrai classement Google (pas de géociblage FR garanti, pas de personnalisation, backend différent) — les positions "#1" à "#5" rapportées jour après jour montrent une variance trop forte pour être un vrai ranking Google. À traiter comme un signal directionnel faible, pas une mesure de position fiable. Search Console (si accès propriétaire) donnerait une mesure correcte — recommandation déjà faite les semaines précédentes.

## IndexNow

`npm run indexnow` exécuté après le push (131 URLs) : `api.indexnow.org` et `www.bing.com` toujours **403 `UserForbiddedToAccessSite`** (persistant depuis fin août — nécessite une ré-authentification du fichier clé côté Bing Webmaster Tools par le propriétaire). Yandex : **202/200 accepté**.

## Dette signalée

- Le gap "markdown rédigé mais jamais publié" par le système `blog-writer` s'est reproduit une 4ᵉ fois — recommandation répétée (31/08, 07/09, 13/09, 14/09) : brancher `check-seo-coverage.cjs` (ou un check équivalent sur `docs/blog-articles/`) en fin de run `blog-writer` pour empêcher la récidive.
- Connecteur Vercel MCP à reconnecter (403/404 sur ce projet alors que le déploiement réel fonctionne, vérifié par `gh api`).
- Bing IndexNow bloqué depuis fin août — action propriétaire requise (ré-vérification du site sur Bing Webmaster Tools).

## Next action (mardi 15/09)

Rotation mardi = boost d'un article existant faible rank. Après la mesure de ce jour, `marge-beneficiaire-restaurant` ou une des pages informationnelles ciblées par l'audit du 07/07 (food cost, menu engineering, prime cost) sont de bons candidats pour +1000 mots / FAQ / maillage interne renforcé, conformément à la priorité "pousser l'existant" toujours active.
