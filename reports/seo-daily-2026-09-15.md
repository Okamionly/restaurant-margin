# SEO Daily Push — 2026-09-15 (mardi)

## Action réalisée : Boost article existant (rotation mardi)

Candidat choisi par taille de fichier (`wc -l client/src/pages/Blog*.tsx | sort -n`) : `BlogOuvrirTerrasseRestaurant.tsx` (237 lignes, plus petit candidat non encore boosté — `BlogAllergenes.tsx` avait déjà été traité le 08/09).

**Ajouts** :
- 2 nouvelles sections (~700 mots) : « Terrasse en hiver : chauffage et rentabilité hors saison » (décret loi Climat 30/03/2022 sur l'interdiction des chauffages gaz/électriques en terrasse ouverte) et « Assurance et responsabilité : ce qui est vraiment couvert » (extension RC occupation de voirie).
- FAQ visible étendue de 4 à 10 Q/R (6 nouvelles questions).
- **Schema `FAQPage` ajouté** (absent auparavant — seul `Article` existait sur cette page).
- 5 liens internes ajoutés : `/blog/augmenter-ticket-moyen-restaurant`, `/blog/seuil-rentabilite-restaurant` (×2), `/blog/gestion-tresorerie-restaurant`, `/blog/reduire-facture-energie-restaurant`, `/blog/kpi-restaurateur`.
- `dateModified`/`lastmod`/`updatedDate` → 2026-09-15.
- `seoBody` ajoutée dans `prerender.cjs` (dupliquant FAQ + nouvelles sections) — cette page n'en avait pas, pattern validé à plusieurs reprises sur ce repo pour que Googlebot voie le contenu réel dès le premier crawl statique (pas seulement le rendu React côté client).

## Files modified
- `client/src/pages/BlogOuvrirTerrasseRestaurant.tsx`
- `client/public/sitemap.xml`
- `client/scripts/prerender.cjs`

## Commit
`7e9b02c` — seo(boost): renforce BlogOuvrirTerrasseRestaurant +2 sections, FAQ 4->10, schema FAQPage, 5 liens internes

## Vercel deploy status
✅ **success** sur les 2 contextes (`Vercel – restaurant-margin` et `Vercel – restaumargin`), vérifié via `gh api repos/Okamionly/restaurant-margin/commits/<sha>/status` (connecteur MCP Vercel toujours cassé sur ce projet — 403/404 depuis mi-août, cf. mémoire `project_restaumargin_routines_health`).

## Vérification live (Googlebot UA)
- `<title>` correct sur la page prod.
- Schema `FAQPage` présent (4 occurrences détectées dans le HTML statique).
- Marqueur du nouveau contenu (« décret du 30 mars 2022 ») présent dans le HTML servi au premier crawl → `seoBody` bien prise en compte.
- Sitemap prod affiche `lastmod` 2026-09-15 à jour.

## IndexNow
- Bing/api.indexnow.org : **403 `UserForbiddedToAccessSite`** — état inchangé, cause actée (site non « claim » dans Bing Webmaster Tools, action externe côté Youssef, PAS un problème de fichier clé).
- Yandex : **202 Accepted** — seul canal fonctionnel actuellement.

## check-seo-coverage.cjs
132 URLs sitemap / 131 routes prerender → OK (écart normal, cf. script).

## Contexte découvert ce run — anti-doublon
Un système multi-agent distinct (« workforce » : cto/coo/cmo/qa/ceo/outreach/onboarder/bug-fixer/seo-writer) commite déjà quotidiennement sur ce repo. Aujourd'hui il avait déjà publié une nouvelle page niche `/guide-marge/bistro-gastronomique` (seo-writer) avant ce run — aucun conflit car la rotation du jour (mardi = boost, pas nouvel article) portait sur un fichier différent. Le COO du workforce a signalé « infra degraded (DB error) » aujourd'hui — probablement le faux positif connu de `/api/health` (cache figé, cf. mémoire `project_restaumargin_routines_health`), sans impact sur ce run (changements statiques, build/déploiement indépendants de l'état DB runtime).

## Position Google
Non re-mesurée ce run (rotation mardi = boost, pas audit positions — le mercredi est dédié à l'audit SERP via Playwright, méthode WebSearch jugée non fiable pour ce cas, cf. mémoire `project_restaumargin_seo_positions`).

## Next action (mercredi 16/09)
Rotation "audit positions Google + analyse concurrent" : scraper les 5 requêtes clés via Playwright MCP (`google.com/search?gl=fr&hl=fr`), analyser 1 concurrent #1, appliquer ≥1 amélioration.
